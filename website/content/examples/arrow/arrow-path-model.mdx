---
title: 'Paths'
sidebar_label: Paths
---

import {ArrowPathModelExample} from '@site/src/examples';

Path rendering from nested Arrow XYZM rows, Float64 source rows, and DenseUnion LineString
rows. DenseUnion inputs are normalized one-for-one into the existing path row shape so
generated segment row indices still refer to full-table path rows.

<ArrowPathModelExample />
