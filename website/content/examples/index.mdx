---
title: Examples
description: Explore GPU-native rendering, physical simulation, visual effects, and data processing across WebGPU and WebGL2.
hide_title: true
---

import {ExamplesIndex} from '@site/src/components/examples-index';
import {getExampleThumbnailPath} from '@site/src/example-thumbnails';

<header className="luma-example-catalog-hero" aria-labelledby="luma-example-catalog-title">
  <p className="luma-example-catalog-eyebrow">luma.gl</p>
  <h1 className="luma-example-catalog-title" id="luma-example-catalog-title">Examples</h1>
  <p className="luma-example-catalog-description">Explore interactive examples for rendering, GPU compute, data processing, and visualization.</p>
</header>

<ExamplesIndex
  getThumbnail={item => {
    const exampleName = typeof item === 'string' ? item : item.docId || item.label.toLowerCase();
    return `/images/examples/${getExampleThumbnailPath(exampleName)}`
  }}
/>
