---
title: Examples
description: Explore GPU-native rendering, physical simulation, visual effects, and data processing across WebGPU and WebGL2.
hide_title: true
---

import {ExamplesIndex} from '@site/src/components/examples-index';

<header className="luma-example-catalog-hero" aria-labelledby="luma-example-catalog-title">
  <p className="luma-example-catalog-eyebrow">The real-time GPU laboratory</p>
  <h1 className="luma-example-catalog-title" id="luma-example-catalog-title">See what the GPU makes possible.</h1>
  <p className="luma-example-catalog-description">Explore living worlds, physical simulations, visual effects, and GPU-native data—from a first triangle to a million moving points.</p>
  <ul className="luma-example-catalog-capabilities" aria-label="Example capabilities">
    <li className="luma-example-catalog-capability">WebGPU</li>
    <li className="luma-example-catalog-capability">WebGL2</li>
    <li className="luma-example-catalog-capability">GPU compute</li>
    <li className="luma-example-catalog-capability">HDR</li>
  </ul>
</header>

<ExamplesIndex
  getThumbnail={item => {
    const exampleName = typeof item === 'string' ? item : item.docId || item.label.toLowerCase();
    if (exampleName === 'v10/gpgpu') {
      return '/images/examples/gpu-tables/gpu-vector-storage-particles.jpg'
    }
    if (exampleName === 'experimental/anari-playground') {
      return '/images/examples/experimental/anari-playground.svg'
    }
    return `/images/examples/${exampleName}.jpg`
  }}
/>
