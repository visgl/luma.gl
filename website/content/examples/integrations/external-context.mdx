---
title: External WebGL Context
description: Attach luma.gl to MapLibre's WebGL2 context and render a world-anchored overlay in its frame loop.
sidebar_custom_props:
  backends: [webgl2]
---

import {ExampleHeader, ExampleStage} from '@site/src/react-luma';
import {ExternalContextExample} from '@site/src/examples';

<ExampleStage>
  <ExampleHeader
    title="External WebGL Context"
    sourcePath="examples/integrations/external-context"
    devices={['webgl2']}
  >
    <div>
      <div style={{marginBottom: 12}}>
        This example attaches a <code>WebGLDevice</code> to the WebGL2 context created by MapLibre GL
        JS and renders a luma.gl overlay through the MapLibre render loop.
      </div>
      <div>
        The map uses CARTO basemaps that do not require an access token. The overlay uses the map
        view-projection matrix so it stays anchored in world space.
      </div>
    </div>
  </ExampleHeader>

  <ExternalContextExample />
</ExampleStage>
