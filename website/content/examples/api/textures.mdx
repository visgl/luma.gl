import {TexturesExample} from '@site/src/examples';

# Textures

This example uses loaders.gl to decode standard images and compressed textures, then previews them with luma.gl in a WebGL canvas. It also supports drag-and-drop uploads so you can test local texture assets against the current browser and GPU.

<TexturesExample />
