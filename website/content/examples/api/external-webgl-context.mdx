import {ExternalWebGLContextExample} from '@site/src/examples'

# External WebGL Context

Connect MapLibre's WebGL canvas to luma.gl using the WebGL device attach API.

<ExternalWebGLContextExample />
