// Default Shaders
import vs from './default.vs.glsl';
import fs from './default.fs.glsl';
const defaultUniforms = require('./default-uniforms');

export const SHADERS = {
  vs,
  fs,
  defaultUniforms
};
