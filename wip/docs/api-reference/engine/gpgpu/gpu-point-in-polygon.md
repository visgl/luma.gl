# GPUPointInPolygon

This page was moved out of the live `@luma.gl/engine` API reference because current engine source no longer exports `GPUPointInPolygon`.

There is no current engine API reference page for this symbol. Keep this file only as an archive placeholder while the old content is retired from the main docs flow.
