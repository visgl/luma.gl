# Transform

This page was moved out of the live `@luma.gl/engine` API reference because current engine source no longer exports a `Transform` class.

Current replacements in live docs:

- [`BufferTransform`](/docs/api-reference/engine/compute/buffer-transform) for WebGL transform-feedback workflows
- [`TextureTransform`](/docs/api-reference/engine/compute/texture-transform) for texture-targeted transform passes
- [`Computation`](/docs/api-reference/engine/compute/computation) for WebGPU compute workloads

Keep this file only as an archive placeholder while the old content is retired from the main docs flow.
