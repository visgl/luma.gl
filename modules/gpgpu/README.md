# @luma.gl/gpgpu

General-purpose GPU computation module for luma.gl.

`@luma.gl/gpgpu` consumes packed single-chunk `GPUVector` inputs from
`@luma.gl/tables` and materializes operation outputs as `GPUVector` objects.

See [luma.gl](https://luma.gl) for documentation.
