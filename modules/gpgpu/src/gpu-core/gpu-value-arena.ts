// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors
import {Buffer} from '@luma.gl/core';import {GPUCommandGraph,GraphDataView,type GraphBufferHandle} from './gpu-command-graph';
export type GPUValueFormat='float32'|'uint32'|'sint32';export const DEFAULT_GPU_VALUE_ARENA_BYTE_LENGTH=16*1024;export type GPUValueSlot<T extends GPUValueFormat=GPUValueFormat>={id:string;format:T;byteOffset:number};const GRAPH_VALUE_ARENAS=new WeakMap<object,GPUValueArena>();
export function getGPUValueArena<Parameters>(graph:GPUCommandGraph<Parameters>):GPUValueArena{const key=graph as unknown as object;let arena=GRAPH_VALUE_ARENAS.get(key);if(!arena){arena=new GPUValueArena(graph);GRAPH_VALUE_ARENAS.set(key,arena);}return arena;}
/** Graph-owned packed storage for small GPU-produced values. Offsets remain stable for graph lifetime. */
export class GPUValueArena{readonly id:string;readonly graph:GPUCommandGraph<unknown>;readonly buffer:GraphBufferHandle;readonly capacityByteLength:number;private nextByteOffset=0;private readonly slots=new Map<string,GPUValueSlot>();private readonly views=new Map<string,GraphDataView>();constructor<Parameters>(graph:GPUCommandGraph<Parameters>,id='gpu-value-arena',capacityByteLength=DEFAULT_GPU_VALUE_ARENA_BYTE_LENGTH){this.id=id;this.graph=graph as unknown as GPUCommandGraph<unknown>;if(!Number.isSafeInteger(capacityByteLength)||capacityByteLength<=0||capacityByteLength%4!==0)throw new Error(`${this.id} capacityByteLength must be a positive multiple of 4`);this.capacityByteLength=capacityByteLength;this.buffer=graph.createTransientBuffer({id:`${this.id}-buffer`,byteLength:capacityByteLength,usage:Buffer.STORAGE|Buffer.COPY_SRC|Buffer.COPY_DST});}allocate<T extends GPUValueFormat>(id:string,format:T):GPUValueSlot<T>{if(!id)throw new Error(`${this.id} slot id must not be empty`);if(this.slots.has(id))throw new Error(`${this.id} slot "${id}" already exists`);if(!['float32','uint32','sint32'].includes(format))throw new Error(`${this.id} unsupported slot format ${format}`);if(this.nextByteOffset+4>this.capacityByteLength)throw new Error(`${this.id} capacity exceeded while allocating "${id}"`);const slot:GPUValueSlot<T>=Object.freeze({id,format,byteOffset:this.nextByteOffset});this.nextByteOffset+=4;this.slots.set(id,slot);this.views.set(id,new GraphDataView(this.buffer,{format,length:1,byteOffset:slot.byteOffset,byteStride:4,rowByteLength:4}));return slot;}get(id:string):GPUValueSlot|undefined{return this.slots.get(id);}getSlots():readonly GPUValueSlot[]{return Object.freeze([...this.slots.values()]);}getView<T extends GPUValueFormat>(slot:GPUValueSlot<T>):GraphDataView<T>{const view=this.views.get(slot.id);if(!view)throw new Error(`${this.id} does not contain slot "${slot.id}"`);return view as GraphDataView<T>;}get byteLength():number{return this.nextByteOffset;}get size():number{return this.slots.size;}get availableByteLength():number{return this.capacityByteLength-this.nextByteOffset;}}
