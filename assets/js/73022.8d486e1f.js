"use strict";(self.webpackChunkwebsite_docusaurus=self.webpackChunkwebsite_docusaurus||[]).push([["73022"],{42196(e,t,n){n.r(t),n.d(t,{makePostprocessingUniforms:()=>U,default:()=>w,updateEffectPassNames:()=>A,flattenEffectSettings:()=>N,getInitialPostprocessingPassNames:()=>_,unflattenEffectSettings:()=>W,makePostprocessingSettingsState:()=>D,reorderEffectPassNames:()=>R,getEffectResolutionScale:()=>B});var o=n(68523),r=n(95222),a=n(21803),i=n(90466),s=n(1344);let c="const GAUSSIAN_BLUR_MAX_RADIUS = ".concat(32,".0;\nconst GAUSSIAN_BLUR_MAX_PAIRS = ").concat(Math.ceil(16),";\n\nstruct gaussianBlurUniforms {\n  radius: f32,\n  delta: vec2f,\n};\n\n@group(0) @binding(auto) var<uniform> gaussianBlur: gaussianBlurUniforms;\n\nfn gaussianBlur_applySample(color: vec4f) -> vec4f {\n  return vec4f(color.rgb * vec3f(color.a), color.a);\n}\n\nfn gaussianBlur_getEffectiveRadius() -> f32 {\n  return clamp(gaussianBlur.radius, 0.0, GAUSSIAN_BLUR_MAX_RADIUS);\n}\n\nfn gaussianBlur_getSigma(radius: f32) -> f32 {\n  return max(radius / 3.0, 0.00001);\n}\n\nfn gaussianBlur_getWeight(offset: f32, sigma: f32) -> f32 {\n  let normalizedOffset = offset / sigma;\n  return exp(-0.5 * normalizedOffset * normalizedOffset);\n}\n\nfn gaussianBlur_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  let effectiveRadius = gaussianBlur_getEffectiveRadius();\n  if (effectiveRadius <= 0.0) {\n    return textureSample(sourceTexture, sourceTextureSampler, texCoord);\n  }\n\n  let adjustedDelta = gaussianBlur.delta / texSize;\n  let sigma = gaussianBlur_getSigma(effectiveRadius);\n  let centerWeight = gaussianBlur_getWeight(0.0, sigma);\n\n  var color = vec4f(0.0);\n  var totalWeight = centerWeight;\n\n  let centerColor = gaussianBlur_applySample(\n    textureSample(sourceTexture, sourceTextureSampler, texCoord)\n  );\n  color += centerColor * centerWeight;\n\n  for (var pairIndex = 0; pairIndex < GAUSSIAN_BLUR_MAX_PAIRS; pairIndex += 1) {\n    let firstOffset = f32(pairIndex * 2 + 1);\n    if (firstOffset > effectiveRadius) {\n      continue;\n    }\n\n    let secondOffset = firstOffset + 1.0;\n    let firstWeight = gaussianBlur_getWeight(firstOffset, sigma);\n    let secondWeight =\n      select(0.0, gaussianBlur_getWeight(secondOffset, sigma), secondOffset <= effectiveRadius);\n    let combinedWeight = firstWeight + secondWeight;\n    let combinedOffset =\n      (firstOffset * firstWeight + secondOffset * secondWeight) / max(combinedWeight, 0.00001);\n\n    let positiveColor = gaussianBlur_applySample(\n      textureSample(sourceTexture, sourceTextureSampler, texCoord + adjustedDelta * combinedOffset)\n    );\n    let negativeColor = gaussianBlur_applySample(\n      textureSample(sourceTexture, sourceTextureSampler, texCoord - adjustedDelta * combinedOffset)\n    );\n\n    color += (positiveColor + negativeColor) * combinedWeight;\n    totalWeight += combinedWeight * 2.0;\n  }\n\n  color /= totalWeight;\n  let unpremultipliedRgb = color.rgb / vec3f(color.a + 0.00001);\n\n  return vec4f(unpremultipliedRgb, color.a);\n}\n"),l="#define GAUSSIAN_BLUR_MAX_RADIUS ".concat(32,".0\n#define GAUSSIAN_BLUR_MAX_PAIRS ").concat(Math.ceil(16),"\n\nlayout(std140) uniform gaussianBlurUniforms {\n  float radius;\n  vec2 delta;\n} gaussianBlur;\n\nvec4 gaussianBlur_applySample(vec4 color) {\n  return vec4(color.rgb * color.a, color.a);\n}\n\nfloat gaussianBlur_getEffectiveRadius() {\n  return clamp(gaussianBlur.radius, 0.0, GAUSSIAN_BLUR_MAX_RADIUS);\n}\n\nfloat gaussianBlur_getSigma(float radius) {\n  return max(radius / 3.0, 0.00001);\n}\n\nfloat gaussianBlur_getWeight(float offset, float sigma) {\n  float normalizedOffset = offset / sigma;\n  return exp(-0.5 * normalizedOffset * normalizedOffset);\n}\n\nvec4 gaussianBlur_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  float effectiveRadius = gaussianBlur_getEffectiveRadius();\n  if (effectiveRadius <= 0.0) {\n    return texture(source, texCoord);\n  }\n\n  vec2 adjustedDelta = gaussianBlur.delta / texSize;\n  float sigma = gaussianBlur_getSigma(effectiveRadius);\n  float centerWeight = gaussianBlur_getWeight(0.0, sigma);\n\n  vec4 color = vec4(0.0);\n  float totalWeight = centerWeight;\n\n  vec4 centerColor = gaussianBlur_applySample(texture(source, texCoord));\n  color += centerColor * centerWeight;\n\n  for (int pairIndex = 0; pairIndex < GAUSSIAN_BLUR_MAX_PAIRS; pairIndex++) {\n    float firstOffset = float(pairIndex * 2 + 1);\n    if (firstOffset > effectiveRadius) {\n      continue;\n    }\n\n    float secondOffset = firstOffset + 1.0;\n    float firstWeight = gaussianBlur_getWeight(firstOffset, sigma);\n    float secondWeight = secondOffset <= effectiveRadius ? gaussianBlur_getWeight(secondOffset, sigma) : 0.0;\n    float combinedWeight = firstWeight + secondWeight;\n    float combinedOffset =\n      (firstOffset * firstWeight + secondOffset * secondWeight) / max(combinedWeight, 0.00001);\n\n    vec4 positiveColor = gaussianBlur_applySample(texture(source, texCoord + adjustedDelta * combinedOffset));\n    vec4 negativeColor = gaussianBlur_applySample(texture(source, texCoord - adjustedDelta * combinedOffset));\n    color += (positiveColor + negativeColor) * combinedWeight;\n    totalWeight += combinedWeight * 2.0;\n  }\n\n  color /= totalWeight;\n  color.rgb /= color.a + 0.00001;\n\n  return color;\n}\n"),f={name:"random",source:"fn random(scale: vec3f, seed: f32) -> f32 {\n  return fract(sin(dot(scale + vec3f(seed), vec3f(12.9898, 78.233, 151.7182))) * 43758.5453 + seed);\n}\n",fs:"float random(vec3 scale, float seed) {\n  /* use the fragment position for a different seed per-pixel */\n  return fract(sin(dot(gl_FragCoord.xyz + seed, scale)) * 43758.5453 + seed);\n}\n"},u={name:"warp",source:"fn warp_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  coord: vec2f\n) -> vec4f {\n  var color = textureSample(sourceTexture, sourceTextureSampler, coord / texSize);\n  let clampedCoord = clamp(coord, vec2f(0.0), texSize);\n  if (any(coord != clampedCoord)) {\n    /* fade to transparent if we are outside the image */\n    color.a *= max(0.0, 1.0 - length(coord - clampedCoord));\n  }\n  return color;\n}\n",fs:"vec4 warp_sampleColor(sampler2D source, vec2 texSize, vec2 coord) {\n  vec4 color = texture(source, coord / texSize);\n  vec2 clampedCoord = clamp(coord, vec2(0.0), texSize);\n  if (coord != clampedCoord) {\n    /* fade to transparent if we are outside the image */\n    color.a *= max(0.0, 1.0 - length(coord - clampedCoord));\n  }\n  return color;\n}\n",passes:[]};var d=n(19086);let p=["Color & Tone","Blur & Glow","Stylize","Warp & Lens","Finish & Detail"],g={brightnessContrast:{name:"brightnessContrast",source:"struct brightnessContrastUniforms {\n  brightness: f32,\n  contrast: f32\n};\n\n@group(0) @binding(auto) var<uniform> brightnessContrast : brightnessContrastUniforms;\n\nfn brightnessContrast_filterColor_ext(color: vec4f, texSize: vec2<f32>, texCoords: vec2<f32>) -> vec4f {\n  var resultRgb = color.rgb + vec3f(brightnessContrast.brightness);\n  if (brightnessContrast.contrast > 0.0) {\n    resultRgb = (resultRgb - vec3f(0.5)) / (1.0 - brightnessContrast.contrast) + vec3f(0.5);\n  } else {\n    resultRgb = (resultRgb - vec3f(0.5)) * (1.0 + brightnessContrast.contrast) + vec3f(0.5);\n  }\n  return vec4f(resultRgb, color.a);\n}\n",fs:"layout(std140) uniform brightnessContrastUniforms {\n  float brightness;\n  float contrast;\n} brightnessContrast;\n\nvec4 brightnessContrast_filterColor(vec4 color) {\n  color.rgb += brightnessContrast.brightness;\n  if (brightnessContrast.contrast > 0.0) {\n    color.rgb = (color.rgb - 0.5) / (1.0 - brightnessContrast.contrast) + 0.5;\n  } else {\n    color.rgb = (color.rgb - 0.5) * (1.0 + brightnessContrast.contrast) + 0.5;\n  }\n  return color;\n}\n\nvec4 brightnessContrast_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  return brightnessContrast_filterColor(color);\n}\n",props:{},uniformTypes:{brightness:"f32",contrast:"f32"},defaultUniforms:{brightness:0,contrast:0},propTypes:{brightness:{format:"f32",value:0,min:-1,max:1},contrast:{format:"f32",value:0,min:-1,max:1}},passes:[{filter:!0}]},hueSaturation:{props:{},name:"hueSaturation",source:"struct hueSaturationUniforms {\n  hue: f32,\n  saturation: f32,\n};\n\n@group(0) @binding(auto) var<uniform> hueSaturation: hueSaturationUniforms;\n\nfn hueSaturation_filterColor(color: vec4f) -> vec4f {\n  let angle = hueSaturation.hue * 3.14159265;\n  let s = sin(angle);\n  let c = cos(angle);\n  let weights = (vec3f(2.0 * c, -sqrt(3.0) * s - c, sqrt(3.0) * s - c) + vec3f(1.0)) / 3.0;\n\n  var resultRgb = vec3f(\n    dot(color.rgb, weights.xyz),\n    dot(color.rgb, weights.zxy),\n    dot(color.rgb, weights.yzx)\n  );\n\n  let average = (resultRgb.r + resultRgb.g + resultRgb.b) / 3.0;\n  if (hueSaturation.saturation > 0.0) {\n    resultRgb += (vec3f(average) - resultRgb) * (1.0 - 1.0 / (1.001 - hueSaturation.saturation));\n  } else {\n    resultRgb += (vec3f(average) - resultRgb) * (-hueSaturation.saturation);\n  }\n\n  return vec4f(resultRgb, color.a);\n}\n\nfn hueSaturation_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  return hueSaturation_filterColor(color);\n}\n",fs:"layout(std140) uniform hueSaturationUniforms {\n  float hue;\n  float saturation;\n} hueSaturation;\n\nvec4 hueSaturation_filterColor(vec4 color) {\n  // hue adjustment, wolfram alpha: RotationTransform[angle, {1, 1, 1}][{x, y, z}]\n  float angle = hueSaturation.hue * 3.14159265;\n  float s = sin(angle), c = cos(angle);\n  vec3 weights = (vec3(2.0 * c, -sqrt(3.0) * s - c, sqrt(3.0) * s - c) + 1.0) / 3.0;\n  float len = length(color.rgb);\n  color.rgb = vec3(\n    dot(color.rgb, weights.xyz),\n    dot(color.rgb, weights.zxy),\n    dot(color.rgb, weights.yzx)\n  );\n\n  // saturation adjustment\n  float average = (color.r + color.g + color.b) / 3.0;\n  if (hueSaturation.saturation > 0.0) {\n    color.rgb += (average - color.rgb) * (1.0 - 1.0 / (1.001 - hueSaturation.saturation));\n  } else {\n    color.rgb += (average - color.rgb) * (-hueSaturation.saturation);\n  }\n\n  return color;\n}\n\nvec4 hueSaturation_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  return hueSaturation_filterColor(color);\n}\n",uniformTypes:{hue:"f32",saturation:"f32"},propTypes:{hue:{value:0,min:-1,max:1},saturation:{value:0,min:-1,max:1}},passes:[{filter:!0}]},sepia:{props:{},uniforms:{},name:"sepia",uniformTypes:{amount:"f32"},propTypes:{amount:{value:.5,min:0,max:1}},fs:"layout(std140) uniform sepiaUniforms {\n  float amount;\n} sepia;\n\nvec4 sepia_filterColor(vec4 color) {\n  float r = color.r;\n  float g = color.g;\n  float b = color.b;\n\n  color.r =\n    min(1.0, (r * (1.0 - (0.607 * sepia.amount))) + (g * (0.769 * sepia.amount)) + (b * (0.189 * sepia.amount)));\n  color.g = min(1.0, (r * 0.349 * sepia.amount) + (g * (1.0 - (0.314 * sepia.amount))) + (b * 0.168 * sepia.amount));\n  color.b = min(1.0, (r * 0.272 * sepia.amount) + (g * 0.534 * sepia.amount) + (b * (1.0 - (0.869 * sepia.amount))));\n\n  return color;\n}\n\nvec4 sepia_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  return sepia_filterColor(color);\n}\n",source:"struct sepiaUniforms {\n  amount: f32,\n};\n\n@group(0) @binding(auto) var<uniform> sepia: sepiaUniforms;\n\nfn sepia_filterColor(color: vec4f) -> vec4f {\n  let r = color.r;\n  let g = color.g;\n  let b = color.b;\n\n  var result = color;\n  result.r =\n    min(1.0, (r * (1.0 - (0.607 * sepia.amount))) + (g * (0.769 * sepia.amount)) + (b * (0.189 * sepia.amount)));\n  result.g = min(1.0, (r * 0.349 * sepia.amount) + (g * (1.0 - (0.314 * sepia.amount))) + (b * 0.168 * sepia.amount));\n  result.b = min(1.0, (r * 0.272 * sepia.amount) + (g * 0.534 * sepia.amount) + (b * (1.0 - (0.869 * sepia.amount))));\n\n  return result;\n}\n\nfn sepia_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  return sepia_filterColor(color);\n}\n",passes:[{filter:!0}]},toneMapping:i.L,vibrance:{props:{},uniforms:{},name:"vibrance",uniformTypes:{amount:"f32"},propTypes:{amount:{value:0,min:-1,max:1}},source:"struct vibranceUniforms {\n  amount: f32,\n};\n\n@group(0) @binding(auto) var<uniform> vibrance: vibranceUniforms;\n\nfn vibrance_filterColor(color: vec4f) -> vec4f {\n  let average = (color.r + color.g + color.b) / 3.0;\n  let mx = max(color.r, max(color.g, color.b));\n  let amt = (mx - average) * (-vibrance.amount * 3.0);\n  let resultRgb = mix(color.rgb, vec3f(mx), amt);\n  return vec4f(resultRgb, color.a);\n}\n\nfn vibrance_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  return vibrance_filterColor(color);\n}\n",fs:"layout(std140) uniform vibranceUniforms {\n  float amount;\n} vibrance;\n\nvec4 vibrance_filterColor(vec4 color) {\n  float average = (color.r + color.g + color.b) / 3.0;\n  float mx = max(color.r, max(color.g, color.b));\n  float amt = (mx - average) * (-vibrance.amount * 3.0);\n  color.rgb = mix(color.rgb, vec3(mx), amt);\n  return color;\n}\n\nvec4 vibrance_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  return vibrance_filterColor(color);\n}\n",passes:[{filter:!0}]},bloom:s.K,gaussianBlur:{name:"gaussianBlur",source:c,fs:l,props:{},uniforms:{},uniformTypes:{radius:"f32",delta:"vec2<f32>"},propTypes:{radius:{value:12,min:0,max:32,softMax:32},delta:{value:[1,0],private:!0}},passes:[{sampler:!0,uniforms:{delta:[1,0]}},{sampler:!0,uniforms:{delta:[0,1]}}]},tiltShift:{name:"tiltShift",dependencies:[f],source:"struct tiltShiftUniforms {\n  blurRadius: f32,\n  gradientRadius: f32,\n  start: vec2f,\n  end: vec2f,\n  invert: u32,\n};\n\n@group(0) @binding(auto) var<uniform> tiltShift: tiltShiftUniforms;\n\nfn tiltShift_getDelta(texSize: vec2f) -> vec2f {\n  let vector = normalize((tiltShift.end - tiltShift.start) * texSize);\n  return select(vector, vec2f(-vector.y, vector.x), tiltShift.invert != 0u);\n}\n\nfn tiltShift_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  var color = vec4f(0.0);\n  var total = 0.0;\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  let offset = random(vec3f(12.9898, 78.233, 151.7182), 0.0);\n\n  let normal = normalize(\n    vec2f(\n      (tiltShift.start.y - tiltShift.end.y) * texSize.y,\n      (tiltShift.end.x - tiltShift.start.x) * texSize.x\n    )\n  );\n  let radius =\n    smoothstep(\n      0.0,\n      1.0,\n      abs(dot(texCoord * texSize - tiltShift.start * texSize, normal)) / tiltShift.gradientRadius\n    ) * tiltShift.blurRadius;\n\n  for (var t = -30.0; t <= 30.0; t += 1.0) {\n    let percent = (t + offset - 0.5) / 30.0;\n    let weight = 1.0 - abs(percent);\n    let offsetColor = textureSample(\n      sourceTexture,\n      sourceTextureSampler,\n      texCoord + tiltShift_getDelta(texSize) / texSize * percent * radius\n    );\n\n    color += offsetColor * weight;\n    total += weight;\n  }\n\n  color /= total;\n  return color;\n}\n",fs:"layout(std140) uniform tiltShiftUniforms {\n  float blurRadius;\n  float gradientRadius;\n  vec2 start;\n  vec2 end;\n  bool invert;\n} tiltShift;\n\nvec2 tiltShift_getDelta(vec2 texSize) {\n  vec2 vector = normalize((tiltShift.end - tiltShift.start) * texSize);\n  return tiltShift.invert ? vec2(-vector.y, vector.x) : vector;\n}\n\nvec4 tiltShift_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec4 color = vec4(0.0);\n  float total = 0.0;\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  float offset = random(vec3(12.9898, 78.233, 151.7182), 0.0);\n\n  vec2 normal = normalize(vec2((tiltShift.start.y - tiltShift.end.y) * texSize.y, (tiltShift.end.x - tiltShift.start.x) * texSize.x));\n  float radius = smoothstep(0.0, 1.0,\n    abs(dot(texCoord * texSize - tiltShift.start * texSize, normal)) / tiltShift.gradientRadius) * tiltShift.blurRadius;\n\n  for (float t = -30.0; t <= 30.0; t++) {\n    float percent = (t + offset - 0.5) / 30.0;\n    float weight = 1.0 - abs(percent);\n    vec4 offsetColor = texture(source, texCoord + tiltShift_getDelta(texSize) / texSize * percent * radius);\n    color += offsetColor * weight;\n    total += weight;\n  }\n\n  color = color / total;\n  return color;\n}\n",props:{},uniforms:{},uniformTypes:{blurRadius:"f32",gradientRadius:"f32",start:"vec2<f32>",end:"vec2<f32>",invert:"i32"},propTypes:{blurRadius:{value:15,min:0,max:50},gradientRadius:{value:200,min:0,max:400},start:{value:[0,0]},end:{value:[1,1]},invert:{value:0,private:!0}},passes:[{sampler:!0,uniforms:{invert:0}},{sampler:!0,uniforms:{invert:1}}]},triangleBlur:{name:"triangleBlur",dependencies:[f],source:"struct triangleBlurUniforms {\n  radius: f32,\n  delta: vec2f,\n};\n\n@group(0) @binding(auto) var<uniform> triangleBlur: triangleBlurUniforms;\n\nfn triangleBlur_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  let adjustedDelta = triangleBlur.delta * triangleBlur.radius / texSize;\n\n  var color = vec4f(0.0);\n  var total = 0.0;\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  let offset = random(vec3f(12.9898, 78.233, 151.7182), 0.0);\n\n  for (var t = -30.0; t <= 30.0; t += 1.0) {\n    let percent = (t + offset - 0.5) / 30.0;\n    let weight = 1.0 - abs(percent);\n    let offsetColor = textureSample(\n      sourceTexture,\n      sourceTextureSampler,\n      texCoord + adjustedDelta * percent\n    );\n\n    /* switch to pre-multiplied alpha to correctly blur transparent images */\n    let premultipliedOffsetColor = vec4f(offsetColor.rgb * vec3f(offsetColor.a), offsetColor.a);\n\n    color += premultipliedOffsetColor * weight;\n    total += weight;\n  }\n\n  color /= total;\n\n  /* switch back from pre-multiplied alpha */\n  let unpremultipliedRgb = color.rgb / vec3f(color.a + 0.00001);\n\n  return vec4f(unpremultipliedRgb, color.a);\n}\n",fs:"layout(std140) uniform triangleBlurUniforms {\n  float radius;\n  vec2 delta;\n} triangleBlur;\n\nvec4 triangleBlur_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec2 adjustedDelta = triangleBlur.delta * triangleBlur.radius / texSize;\n\n  vec4 color = vec4(0.0);\n  float total = 0.0;\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  float offset = random(vec3(12.9898, 78.233, 151.7182), 0.0);\n\n  for (float t = -30.0; t <= 30.0; t++) {\n    float percent = (t + offset - 0.5) / 30.0;\n    float weight = 1.0 - abs(percent);\n    vec4 offsetColor = texture(source, texCoord + adjustedDelta * percent);\n\n    /* switch to pre-multiplied alpha to correctly blur transparent images */\n    offsetColor.rgb *= offsetColor.a;\n\n    color += offsetColor * weight;\n    total += weight;\n  }\n\n  color = color / total;\n\n  /* switch back from pre-multiplied alpha */\n  color.rgb /= color.a + 0.00001;\n\n  return color;\n}\n",props:{},uniforms:{},uniformTypes:{radius:"f32",delta:"vec2<f32>"},propTypes:{radius:{value:20,min:0,softMax:100},delta:{value:[1,0],private:!0}},passes:[{sampler:!0,uniforms:{delta:[1,0]}},{sampler:!0,uniforms:{delta:[0,1]}}]},zoomBlur:{name:"zoomBlur",dependencies:[f],source:"\nstruct zoomBlurUniforms {\n  center: vec2f,\n  strength: f32,\n};\n\n@group(0) @binding(auto) var<uniform> zoomBlur : zoomBlurUniforms;\n\n\nfn zoomBlur_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  var color = vec4f(0.0);\n  var total = 0.0;\n  let toCenter = zoomBlur.center * texSize - texCoord * texSize;\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  let offset = random(vec3f(12.9898, 78.233, 151.7182), 0.0);\n\n  for (var t = 0.0; t <= 40.0; t += 1.0) {\n    let percent = (t + offset) / 40.0;\n    let weight = 4.0 * (percent - percent * percent);\n    let offsetColor = textureSample(\n      sourceTexture,\n      sourceTextureSampler,\n      texCoord + toCenter * percent * zoomBlur.strength / texSize\n    );\n    color += offsetColor * weight;\n    total += weight;\n  }\n\n  color /= total;\n  return color;\n}\n",fs:"\nlayout(std140) uniform zoomBlurUniforms {\n  vec2 center;\n  float strength;\n} zoomBlur;\n\nvec4 zoomBlur_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec4 color = vec4(0.0);\n  float total = 0.0;\n  vec2 toCenter = zoomBlur.center * texSize - texCoord * texSize;\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  float offset = random(vec3(12.9898, 78.233, 151.7182), 0.0);\n\n  for (float t = 0.0; t <= 40.0; t++) {\n    float percent = (t + offset) / 40.0;\n    float weight = 4.0 * (percent - percent * percent);\n    vec4 offsetColor = texture(source, texCoord + toCenter * percent * zoomBlur.strength / texSize);\n    color += offsetColor * weight;\n    total += weight;\n  }\n\n  color = color / total;\n  return color;\n}\n",props:{},uniforms:{},uniformTypes:{center:"vec2<f32>",strength:"f32"},propTypes:{center:{value:[.5,.5]},strength:{value:.3,min:0,softMax:1}},passes:[{sampler:!0}]},colorHalftone:{name:"colorHalftone",source:"struct colorHalftoneUniforms {\n  center: vec2f,\n  angle: f32,\n  size: f32,\n};\n\n@group(0) @binding(auto) var<uniform> colorHalftone: colorHalftoneUniforms;\n\nfn pattern(angle: f32, scale: f32, texSize: vec2f, texCoord: vec2f) -> f32 {\n  let s = sin(angle);\n  let c = cos(angle);\n  let tex = texCoord * texSize - colorHalftone.center * texSize;\n  let point = vec2f(\n    c * tex.x - s * tex.y,\n    s * tex.x + c * tex.y\n  ) * scale;\n  return (sin(point.x) * sin(point.y)) * 4.0;\n}\n\nfn colorHalftone_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  let scale = 3.1514 / colorHalftone.size;\n  var cmy = vec3f(1.0) - color.rgb;\n  var k = min(cmy.x, min(cmy.y, cmy.z));\n  let safeDenominator = max(1.0 - k, 0.00001);\n\n  cmy = (cmy - vec3f(k)) / safeDenominator;\n  cmy = clamp(\n    cmy * 10.0 - vec3f(3.0) + vec3f(\n      pattern(colorHalftone.angle + 0.26179, scale, texSize, texCoord),\n      pattern(colorHalftone.angle + 1.30899, scale, texSize, texCoord),\n      pattern(colorHalftone.angle, scale, texSize, texCoord)\n    ),\n    vec3f(0.0),\n    vec3f(1.0)\n  );\n  k = clamp(k * 10.0 - 5.0 + pattern(colorHalftone.angle + 0.78539, scale, texSize, texCoord), 0.0, 1.0);\n  return vec4f(vec3f(1.0) - cmy - vec3f(k), color.a);\n}\n",fs:"layout(std140) uniform colorHalftoneUniforms {\n  vec2 center;\n  float angle;\n  float size;\n} colorHalftone;\n\nfloat pattern(float angle, float scale, vec2 texSize, vec2 texCoord) {\n  float s = sin(angle), c = cos(angle);\n  vec2 tex = texCoord * texSize - colorHalftone.center * texSize;\n  vec2 point = vec2(\n	c * tex.x - s * tex.y,\n	s * tex.x + c * tex.y\n  ) * scale;\n  return (sin(point.x) * sin(point.y)) * 4.0;\n}\n\nvec4 colorHalftone_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  float scale = 3.1514 / colorHalftone.size;\n  vec3 cmy = 1.0 - color.rgb;\n  float k = min(cmy.x, min(cmy.y, cmy.z));\n\n  cmy = (cmy - k) / (1.0 - k);\n  cmy = clamp(\n	  cmy * 10.0 - 3.0 + vec3(\n      pattern(colorHalftone.angle + 0.26179, scale, texSize, texCoord),\n	    pattern(colorHalftone.angle + 1.30899, scale, texSize, texCoord),\n      pattern(colorHalftone.angle, scale, texSize, texCoord)\n    ),\n	  0.0,\n	  1.0\n  );\n  k = clamp(k * 10.0 - 5.0 + pattern(colorHalftone.angle + 0.78539, scale, texSize, texCoord), 0.0, 1.0);\n  return vec4(1.0 - cmy - k, color.a);\n}\n",props:{},uniforms:{},uniformTypes:{center:"vec2<f32>",angle:"f32",size:"f32"},propTypes:{center:{value:[.5,.5]},angle:{value:1.1,softMin:0,softMax:Math.PI/2},size:{value:4,min:1,softMin:3,softMax:20}},passes:[{filter:!0}]},dotScreen:{name:"dotScreen",source:"struct dotScreenUniforms {\n  center: vec2f,\n  angle: f32,\n  size: f32,\n};\n\n@group(0) @binding(auto) var<uniform> dotScreen: dotScreenUniforms;\n\nfn pattern(texSize: vec2f, texCoord: vec2f) -> f32 {\n  let scale = 3.1415 / dotScreen.size;\n  let s = sin(dotScreen.angle);\n  let c = cos(dotScreen.angle);\n  let tex = texCoord * texSize - dotScreen.center * texSize;\n  let point = vec2f(\n    c * tex.x - s * tex.y,\n    s * tex.x + c * tex.y\n  ) * scale;\n  return (sin(point.x) * sin(point.y)) * 4.0;\n}\n\nfn dotScreen_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  let average = (color.r + color.g + color.b) / 3.0;\n  return vec4f(vec3f(average * 10.0 - 5.0 + pattern(texSize, texCoord)), color.a);\n}\n",fs:"layout(std140) uniform dotScreenUniforms {\n  vec2 center;\n  float angle;\n  float size;\n} dotScreen;\n\nfloat pattern(vec2 texSize, vec2 texCoord) {\n  float scale = 3.1415 / dotScreen.size;\n\n  float s = sin(dotScreen.angle), c = cos(dotScreen.angle);\n  vec2 tex = texCoord * texSize - dotScreen.center * texSize;\n  vec2 point = vec2(\n    c * tex.x - s * tex.y,\n    s * tex.x + c * tex.y\n  ) * scale;\n  return (sin(point.x) * sin(point.y)) * 4.0;\n}\n\nvec4 dotScreen_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  float average = (color.r + color.g + color.b) / 3.0;\n  return vec4(vec3(average * 10.0 - 5.0 + pattern(texSize, texCoord)), color.a);\n}\n",props:{},uniforms:{},uniformTypes:{center:"vec2<f32>",angle:"f32",size:"f32"},propTypes:{center:{value:[.5,.5]},angle:{value:1.1,softMin:0,softMax:Math.PI/2},size:{value:3,min:1,softMin:3,softMax:20}},passes:[{filter:!0}]},edgeWork:{name:"edgeWork",dependencies:[f],source:"struct edgeWorkUniforms {\n  radius: f32,\n  mode: u32,\n};\n\n@group(0) @binding(auto) var<uniform> edgeWork: edgeWorkUniforms;\n\nfn edgeWork_sampleColorRGB(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f,\n  delta: vec2f\n) -> vec4f {\n  let relativeDelta = edgeWork.radius * delta / texSize;\n\n  var color = vec2f(0.0);\n  var total = vec2f(0.0);\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  let offset = random(vec3f(12.9898, 78.233, 151.7182), 0.0);\n\n  for (var t = -30.0; t <= 30.0; t += 1.0) {\n    let percent = (t + offset - 0.5) / 30.0;\n    var weight = 1.0 - abs(percent);\n    let sampleColor = textureSample(\n      sourceTexture,\n      sourceTextureSampler,\n      texCoord + relativeDelta * percent\n    ).rgb;\n    let average = (sampleColor.r + sampleColor.g + sampleColor.b) / 3.0;\n    color.x += average * weight;\n    total.x += weight;\n    if (abs(t) < 15.0) {\n      weight = weight * 2.0 - 1.0;\n      color.y += average * weight;\n      total.y += weight;\n    }\n  }\n  return vec4f(color / total, 0.0, 1.0);\n}\n\nfn edgeWork_sampleColorXY(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f,\n  delta: vec2f\n) -> vec4f {\n  let relativeDelta = edgeWork.radius * delta / texSize;\n\n  var color = vec2f(0.0);\n  var total = vec2f(0.0);\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  let offset = random(vec3f(12.9898, 78.233, 151.7182), 0.0);\n\n  for (var t = -30.0; t <= 30.0; t += 1.0) {\n    let percent = (t + offset - 0.5) / 30.0;\n    var weight = 1.0 - abs(percent);\n    let sampleColor = textureSample(\n      sourceTexture,\n      sourceTextureSampler,\n      texCoord + relativeDelta * percent\n    ).xy;\n    color.x += sampleColor.x * weight;\n    total.x += weight;\n    if (abs(t) < 15.0) {\n      weight = weight * 2.0 - 1.0;\n      color.y += sampleColor.y * weight;\n      total.y += weight;\n    }\n  }\n\n  let c = clamp(10000.0 * (color.y / total.y - color.x / total.x) + 0.5, 0.0, 1.0);\n  return vec4f(c, c, c, 1.0);\n}\n\nfn edgeWork_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  if (edgeWork.mode == 0u) {\n    return edgeWork_sampleColorRGB(\n      sourceTexture,\n      sourceTextureSampler,\n      texSize,\n      texCoord,\n      vec2f(1.0, 0.0)\n    );\n  }\n\n  return edgeWork_sampleColorXY(\n    sourceTexture,\n    sourceTextureSampler,\n    texSize,\n    texCoord,\n    vec2f(0.0, 1.0)\n  );\n}\n",fs:"layout(std140) uniform edgeWorkUniforms {\n  float radius;\n  int mode;\n} edgeWork;\n\nvec4 edgeWork_sampleColorRGB(sampler2D source, vec2 texSize, vec2 texCoord, vec2 delta) {\n  vec2 relativeDelta = edgeWork.radius * delta / texSize;\n\n  vec2 color = vec2(0.0);\n  vec2 total = vec2(0.0);\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  float offset = random(vec3(12.9898, 78.233, 151.7182), 0.0);\n\n  for (float t = -30.0; t <= 30.0; t++) {\n    float percent = (t + offset - 0.5) / 30.0;\n    float weight = 1.0 - abs(percent);\n    vec3 sampleColor = texture(source, texCoord + relativeDelta * percent).rgb;\n    float average = (sampleColor.r + sampleColor.g + sampleColor.b) / 3.0;\n    color.x += average * weight;\n    total.x += weight;\n    if (abs(t) < 15.0) {\n      weight = weight * 2.0 - 1.0;\n      color.y += average * weight;\n      total.y += weight;\n    }\n  }\n  return vec4(color / total, 0.0, 1.0);\n}\n\nvec4 edgeWork_sampleColorXY(sampler2D source, vec2 texSize, vec2 texCoord, vec2 delta) {\n  vec2 relativeDelta = edgeWork.radius * delta / texSize;\n\n  vec2 color = vec2(0.0);\n  vec2 total = vec2(0.0);\n\n  /* randomize the lookup values to hide the fixed number of samples */\n  float offset = random(vec3(12.9898, 78.233, 151.7182), 0.0);\n\n  for (float t = -30.0; t <= 30.0; t++) {\n    float percent = (t + offset - 0.5) / 30.0;\n    float weight = 1.0 - abs(percent);\n    vec2 sampleColor = texture(source, texCoord + relativeDelta * percent).xy;\n    color.x += sampleColor.x * weight;\n    total.x += weight;\n    if (abs(t) < 15.0) {\n      weight = weight * 2.0 - 1.0;\n      color.y += sampleColor.y * weight;\n      total.y += weight;\n    }\n  }\n  float c = clamp(10000.0 * (color.y / total.y - color.x / total.x) + 0.5, 0.0, 1.0);\n  return vec4(c, c, c, 1.0);\n}\n\nvec4 edgeWork_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  switch (edgeWork.mode) {\n    case 0: \n    return edgeWork_sampleColorRGB(source, texSize, texCoord, vec2(1., 0.));\n    case 1: \n    default:\n      return edgeWork_sampleColorXY(source, texSize, texCoord, vec2(0., 1.));\n  }\n}\n",props:{},uniforms:{},uniformTypes:{radius:"f32",mode:"i32"},propTypes:{radius:{value:2,min:1,softMax:50},mode:{value:0,private:!0}},passes:[{sampler:!0,uniforms:{mode:0}},{sampler:!0,uniforms:{mode:1}}]},hexagonalPixelate:{name:"hexagonalPixelate",source:"struct hexagonalPixelateUniforms {\n  center: vec2f,\n  scale: f32,\n};\n\n@group(0) @binding(auto) var<uniform> hexagonalPixelate: hexagonalPixelateUniforms;\n\nfn hexagonalPixelate_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  var tex = (texCoord * texSize - hexagonalPixelate.center * texSize) / hexagonalPixelate.scale;\n  tex.y /= 0.866025404;\n  tex.x -= tex.y * 0.5;\n\n  var a = vec2f(0.0);\n  if (tex.x + tex.y - floor(tex.x) - floor(tex.y) < 1.0) {\n    a = vec2f(floor(tex.x), floor(tex.y));\n  } else {\n    a = vec2f(ceil(tex.x), ceil(tex.y));\n  }\n  let b = vec2f(ceil(tex.x), floor(tex.y));\n  let c = vec2f(floor(tex.x), ceil(tex.y));\n\n  let texBarycentric = vec3f(tex.x, tex.y, 1.0 - tex.x - tex.y);\n  let aBarycentric = vec3f(a.x, a.y, 1.0 - a.x - a.y);\n  let bBarycentric = vec3f(b.x, b.y, 1.0 - b.x - b.y);\n  let cBarycentric = vec3f(c.x, c.y, 1.0 - c.x - c.y);\n\n  let aLength = length(texBarycentric - aBarycentric);\n  let bLength = length(texBarycentric - bBarycentric);\n  let cLength = length(texBarycentric - cBarycentric);\n\n  var choice = vec2f(0.0);\n  if (aLength < bLength) {\n    if (aLength < cLength) {\n      choice = a;\n    } else {\n      choice = c;\n    }\n  } else {\n    if (bLength < cLength) {\n      choice = b;\n    } else {\n      choice = c;\n    }\n  }\n\n  choice.x += choice.y * 0.5;\n  choice.y *= 0.866025404;\n  choice *= hexagonalPixelate.scale / texSize;\n\n  return textureSample(sourceTexture, sourceTextureSampler, choice + hexagonalPixelate.center);\n}\n",fs:"layout(std140) uniform hexagonalPixelateUniforms {\n  vec2 center;\n  float scale;\n} hexagonalPixelate;\n\nvec4 hexagonalPixelate_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec2 tex = (texCoord * texSize - hexagonalPixelate.center * texSize) / hexagonalPixelate.scale;\n  tex.y /= 0.866025404;\n  tex.x -= tex.y * 0.5;\n\n  vec2 a;\n  if (tex.x + tex.y - floor(tex.x) - floor(tex.y) < 1.0) {\n    a = vec2(floor(tex.x), floor(tex.y));\n  }\n  else a = vec2(ceil(tex.x), ceil(tex.y));\n  vec2 b = vec2(ceil(tex.x), floor(tex.y));\n  vec2 c = vec2(floor(tex.x), ceil(tex.y));\n\n  vec3 TEX = vec3(tex.x, tex.y, 1.0 - tex.x - tex.y);\n  vec3 A = vec3(a.x, a.y, 1.0 - a.x - a.y);\n  vec3 B = vec3(b.x, b.y, 1.0 - b.x - b.y);\n  vec3 C = vec3(c.x, c.y, 1.0 - c.x - c.y);\n\n  float alen = length(TEX - A);\n  float blen = length(TEX - B);\n  float clen = length(TEX - C);\n\n  vec2 choice;\n  if (alen < blen) {\n    if (alen < clen) choice = a;\n    else choice = c;\n  } else {\n    if (blen < clen) choice = b;\n    else choice = c;\n  }\n\n  choice.x += choice.y * 0.5;\n  choice.y *= 0.866025404;\n  choice *= hexagonalPixelate.scale / texSize;\n\n  return texture(source, choice + hexagonalPixelate.center);\n}\n",props:{},uniforms:{},uniformTypes:{center:"vec2<f32>",scale:"f32"},propTypes:{center:{value:[.5,.5],hint:"screenspace"},scale:{value:10,min:1,softMin:5,softMax:50}},passes:[{sampler:!0}]},ink:{name:"ink",source:"struct inkUniforms {\n  strength: f32,\n};\n\n@group(0) @binding(auto) var<uniform> ink: inkUniforms;\n\nfn ink_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  let dx = vec2f(1.0 / texSize.x, 0.0);\n  let dy = vec2f(0.0, 1.0 / texSize.y);\n  let color = textureSample(sourceTexture, sourceTextureSampler, texCoord);\n  var bigTotal = 0.0;\n  var smallTotal = 0.0;\n  var bigAverage = vec3f(0.0);\n  var smallAverage = vec3f(0.0);\n  for (var x = -2.0; x <= 2.0; x += 1.0) {\n    for (var y = -2.0; y <= 2.0; y += 1.0) {\n      let offsetColor = textureSample(\n        sourceTexture,\n        sourceTextureSampler,\n        texCoord + dx * x + dy * y\n      ).rgb;\n      bigAverage += offsetColor;\n      bigTotal += 1.0;\n      if (abs(x) + abs(y) < 2.0) {\n        smallAverage += offsetColor;\n        smallTotal += 1.0;\n      }\n    }\n  }\n  let edge = max(vec3f(0.0), bigAverage / bigTotal - smallAverage / smallTotal);\n  let power = ink.strength * ink.strength * ink.strength * ink.strength * ink.strength;\n  return vec4f(color.rgb - vec3f(dot(edge, edge) * power * 100000.0), color.a);\n}\n",fs:"layout(std140) uniform inkUniforms {\n  float strength;\n} ink;\n\nvec4 ink_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec2 dx = vec2(1.0 / texSize.x, 0.0);\n  vec2 dy = vec2(0.0, 1.0 / texSize.y);\n  vec4 color = texture(source, texCoord);\n  float bigTotal = 0.0;\n  float smallTotal = 0.0;\n  vec3 bigAverage = vec3(0.0);\n  vec3 smallAverage = vec3(0.0);\n  for (float x = -2.0; x <= 2.0; x += 1.0) {\n    for (float y = -2.0; y <= 2.0; y += 1.0) {\n      vec3 offsetColor = texture(source, texCoord + dx * x + dy * y).rgb;\n      bigAverage += offsetColor;\n      bigTotal += 1.0;\n      if (abs(x) + abs(y) < 2.0) {\n        smallAverage += offsetColor;\n        smallTotal += 1.0;\n      }\n    }\n  }\n  vec3 edge = max(vec3(0.0), bigAverage / bigTotal - smallAverage / smallTotal);\n  float power = ink.strength * ink.strength * ink.strength * ink.strength * ink.strength;\n  return vec4(color.rgb - dot(edge, edge) * power * 100000.0, color.a);\n}\n",props:{},uniforms:{},uniformTypes:{strength:"f32"},propTypes:{strength:{value:.25,min:0,softMax:1}},passes:[{sampler:!0}]},bulgePinch:{name:"bulgePinch",dependencies:[u],source:"struct bulgePinchUniforms {\n  center: vec2f,\n  radius: f32,\n  strength: f32,\n};\n\n@group(0) @binding(auto) var<uniform> bulgePinch: bulgePinchUniforms;\n\nfn bulgePinch_warp(coordIn: vec2f, texCenter: vec2f) -> vec2f {\n  var coord = coordIn - texCenter;\n  let distance = length(coord);\n  if (distance < bulgePinch.radius) {\n    let percent = distance / bulgePinch.radius;\n    let safeDistance = max(distance, 0.00001);\n    if (bulgePinch.strength > 0.0) {\n      coord *= mix(\n        1.0,\n        smoothstep(0.0, bulgePinch.radius / safeDistance, percent),\n        bulgePinch.strength * 0.75\n      );\n    } else {\n      coord *= mix(\n        1.0,\n        pow(percent, 1.0 + bulgePinch.strength * 0.75) * bulgePinch.radius / safeDistance,\n        1.0 - percent\n      );\n    }\n  }\n  coord += texCenter;\n  return coord;\n}\n\nfn bulgePinch_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  var coord = texCoord * texSize;\n  coord = bulgePinch_warp(coord, bulgePinch.center * texSize);\n  return warp_sampleColor(sourceTexture, sourceTextureSampler, texSize, coord);\n}\n",fs:"layout(std140) uniform bulgePinchUniforms {\n  vec2 center;\n  float radius;\n  float strength;\n} bulgePinch;\n\nvec2 bulgePinch_warp(vec2 coord, vec2 texCenter) {\n  coord -= texCenter;\n  float distance = length(coord);\n  if (distance < bulgePinch.radius) {\n    float percent = distance / bulgePinch.radius;\n    if (bulgePinch.strength > 0.0) {\n      coord *= mix(1.0, smoothstep(0.0, bulgePinch.radius / distance, percent), bulgePinch.strength * 0.75);\n    } else {\n      coord *= mix(1.0, pow(percent, 1.0 + bulgePinch.strength * 0.75) * bulgePinch.radius / distance, 1.0 - percent);\n    }\n  }\n  coord += texCenter;\n  return coord;\n}\n\nvec4 bulgePinch_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec2 coord = texCoord * texSize;\n  coord = bulgePinch_warp(coord, bulgePinch.center * texSize);\n\n  return warp_sampleColor(source, texSize, coord);\n}\n",props:{},uniforms:{},uniformTypes:{center:"vec2<f32>",radius:"f32",strength:"f32"},propTypes:{center:{value:[.5,.5]},radius:{value:200,min:1,softMax:600},strength:{value:.5,min:-1,max:1}},passes:[{sampler:!0}]},magnify:{name:"magnify",source:"struct magnifyUniforms {\n  screenXY: vec2f,\n  radiusPixels: f32,\n  zoom: f32,\n  borderWidthPixels: f32,\n  borderColor: vec4f,\n};\n\n@group(0) @binding(auto) var<uniform> magnify: magnifyUniforms;\n\nfn magnify_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  let pos = vec2f(magnify.screenXY.x, 1.0 - magnify.screenXY.y);\n  let dist = distance(texCoord * texSize, pos * texSize);\n  if (dist < magnify.radiusPixels) {\n    return textureSampleLevel(\n      sourceTexture,\n      sourceTextureSampler,\n      (texCoord - pos) / magnify.zoom + pos,\n      0.0\n    );\n  }\n\n  if (dist <= magnify.radiusPixels + magnify.borderWidthPixels) {\n    return magnify.borderColor;\n  }\n  return textureSampleLevel(sourceTexture, sourceTextureSampler, texCoord, 0.0);\n}\n",fs:"layout(std140) uniform magnifyUniforms {\n  vec2 screenXY;\n  float radiusPixels;\n  float zoom;\n  float borderWidthPixels;\n  vec4 borderColor;\n} magnify;\n\nvec4 magnify_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec2 pos = vec2(magnify.screenXY.x, 1.0 - magnify.screenXY.y);\n  float dist = distance(texCoord * texSize, pos * texSize);\n  if (dist < magnify.radiusPixels) {\n    return texture(source, (texCoord - pos) / magnify.zoom + pos);\n  }\n\n  if (dist <= magnify.radiusPixels + magnify.borderWidthPixels) {\n    return magnify.borderColor;\n  }\n  return texture(source, texCoord);\n}\n",uniformTypes:{screenXY:"vec2<f32>",radiusPixels:"f32",zoom:"f32",borderWidthPixels:"f32",borderColor:"vec4<f32>"},propTypes:{screenXY:{value:[0,0]},radiusPixels:200,zoom:2,borderWidthPixels:0,borderColor:{value:[255,255,255,255]}},passes:[{sampler:!0}]},swirl:{name:"swirl",dependencies:[u],source:"struct swirlUniforms {\n  center: vec2f,\n  radius: f32,\n  angle: f32,\n};\n\n@group(0) @binding(auto) var<uniform> swirl: swirlUniforms;\n\nfn swirl_warp(coordIn: vec2f, texCenter: vec2f) -> vec2f {\n  var coord = coordIn - texCenter;\n  let distance = length(coord);\n  if (distance < swirl.radius) {\n    let percent = (swirl.radius - distance) / swirl.radius;\n    let theta = percent * percent * swirl.angle;\n    let s = sin(theta);\n    let c = cos(theta);\n    coord = vec2f(\n      coord.x * c - coord.y * s,\n      coord.x * s + coord.y * c\n    );\n  }\n  coord += texCenter;\n  return coord;\n}\n\nfn swirl_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  var coord = texCoord * texSize;\n  coord = swirl_warp(coord, swirl.center * texSize);\n  return warp_sampleColor(sourceTexture, sourceTextureSampler, texSize, coord);\n}\n",fs:"layout(std140) uniform swirlUniforms {\n  vec2 center;\n  float radius;\n  float angle;\n} swirl;\n\nvec2 swirl_warp(vec2 coord, vec2 texCenter) {\n  coord -= texCenter;\n  float distance = length(coord);\n  if (distance < swirl.radius) {\n    float percent = (swirl.radius - distance) / swirl.radius;\n    float theta = percent * percent * swirl.angle;\n    float s = sin(theta);\n    float c = cos(theta);\n    coord = vec2(\n      coord.x * c - coord.y * s,\n      coord.x * s + coord.y * c\n    );\n  }\n  coord += texCenter;\n  return coord;\n}\n\nvec4 swirl_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  vec2 coord = texCoord * texSize;\n  coord = swirl_warp(coord, swirl.center * texSize);\n\n  return warp_sampleColor(source, texSize, coord);\n}\n",props:{},uniforms:{},uniformTypes:{center:"vec2<f32>",radius:"f32",angle:"f32"},propTypes:{center:{value:[.5,.5]},radius:{value:200,min:1,softMax:600},angle:{value:3,softMin:-25,softMax:25}},passes:[{sampler:!0}]},denoise:{props:{},uniforms:{},name:"denoise",uniformTypes:{strength:"f32"},propTypes:{strength:{format:"f32",value:.5,min:0,max:1}},source:"struct denoiseUniforms {\n  strength: f32,\n};\n\n@group(0) @binding(auto) var<uniform> denoise: denoiseUniforms;\n\nfn denoise_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  let adjustedExponent = 3.0 + 200.0 * pow(1.0 - denoise.strength, 4.0);\n  let center = textureSample(sourceTexture, sourceTextureSampler, texCoord);\n  var color = vec4f(0.0);\n  var total = 0.0;\n\n  for (var x = -4.0; x <= 4.0; x += 1.0) {\n    for (var y = -4.0; y <= 4.0; y += 1.0) {\n      let offsetColor = textureSample(\n        sourceTexture,\n        sourceTextureSampler,\n        texCoord + vec2f(x, y) / texSize\n      );\n      let weight = pow(\n        1.0 - abs(dot(offsetColor.rgb - center.rgb, vec3f(0.25))),\n        adjustedExponent\n      );\n      color += offsetColor * weight;\n      total += weight;\n    }\n  }\n\n  return color / total;\n}\n",fs:"layout(std140) uniform denoiseUniforms {\n  float strength;\n} denoise;\n\nvec4 denoise_sampleColor(sampler2D source, vec2 texSize, vec2 texCoord) {\n  float adjustedExponent = 3. + 200. * pow(1. - denoise.strength, 4.);\n\n  vec4 center = texture(source, texCoord);\n  vec4 color = vec4(0.0);\n  float total = 0.0;\n  for (float x = -4.0; x <= 4.0; x += 1.0) {\n    for (float y = -4.0; y <= 4.0; y += 1.0) {\n      vec4 offsetColor = texture(source, texCoord + vec2(x, y) / texSize);\n      float weight = 1.0 - abs(dot(offsetColor.rgb - center.rgb, vec3(0.25)));\n      weight = pow(weight, adjustedExponent);\n      color += offsetColor * weight;\n      total += weight;\n    }\n  }\n\n  return color / total;\n}\n",passes:[{sampler:!0},{sampler:!0}]},fxaa:d.d,noise:{name:"noise",fs:"layout(std140) uniform noiseUniforms {\n  float amount;\n} noise;\n\nfloat rand(vec2 co) {\n  return fract(sin(dot(co.xy ,vec2(12.9898,78.233))) * 43758.5453);\n}\n\nvec4 noise_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  float diff = (rand(texCoord) - 0.5) * noise.amount;\n  color.r += diff;\n  color.g += diff;\n  color.b += diff;\n  return color;\n}\n",source:"struct noiseUniforms {\n  amount: f32,\n};\n\n@group(0) @binding(auto) var<uniform> noise: noiseUniforms;\n\nfn rand(co: vec2f) -> f32 {\n	return fract(sin(dot(co.xy, vec2f(12.9898, 78.233))) * 43758.547);\n} \n\nfn noise_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  let diff = (rand(texCoord) - 0.5) * noise.amount;\n  var result = color;\n  result.r += diff;\n  result.g += diff;\n  result.b += diff;\n  return result;\n}\n",props:{},uniforms:{},uniformTypes:{amount:"f32"},propTypes:{amount:{value:.5,min:0,max:1}},passes:[{filter:!0}]},vignette:{props:{},uniforms:{},name:"vignette",source:"struct vignetteUniforms {\n  radius: f32,\n  amount: f32,\n};\n\n@group(0) @binding(auto) var<uniform> vignette: vignetteUniforms;\n\nfn vignette_filterColor_ext(color: vec4f, texSize: vec2f, texCoord: vec2f) -> vec4f {\n  let dist = distance(texCoord, vec2f(0.5, 0.5));\n  let ratio = smoothstep(\n    0.8,\n    vignette.radius * 0.799,\n    dist * (vignette.amount + vignette.radius)\n  );\n  return color * ratio + vec4f(0.0, 0.0, 0.0, 1.0) * (1.0 - ratio);\n}\n",fs:"layout(std140) uniform vignetteUniforms {\n  float radius;\n  float amount;\n} vignette;\n\nvec4 vignette_filterColor_ext(vec4 color, vec2 texSize, vec2 texCoord) {\n  float dist = distance(texCoord, vec2(0.5, 0.5));\n  float ratio = smoothstep(0.8, vignette.radius * 0.799, dist * (vignette.amount + vignette.radius));\n  return color.rgba * ratio + (1.0 - ratio)*vec4(0.0, 0.0, 0.0, 1.0);\n}\n",uniformTypes:{radius:"f32",amount:"f32"},defaultUniforms:{radius:.5,amount:.5},propTypes:{radius:{value:.5,min:0,max:1},amount:{value:.5,min:0,max:1}},passes:[{filter:!0}]}},m={brightnessContrast:{label:"Brightness + Contrast",description:"Shapes the image range before the final presentation.",category:"Color & Tone"},hueSaturation:{label:"Hue + Saturation",description:"Rotates the palette and controls chroma intensity.",category:"Color & Tone"},sepia:{label:"Sepia",description:"Warms the image with a controllable vintage photographic tint.",category:"Color & Tone"},toneMapping:{label:"Tone Mapping",description:"Maps scene brightness through a filmic curve with exposure control.",category:"Color & Tone"},vibrance:{label:"Vibrance",description:"Raises quieter colors while protecting already saturated hues.",category:"Color & Tone"},bloom:{label:"Bloom",description:"Spreads selected highlights into a soft luminous halo.",category:"Blur & Glow"},gaussianBlur:{label:"Gaussian Blur",description:"Softens the image with a smooth, separable Gaussian blur.",category:"Blur & Glow"},tiltShift:{label:"Tilt Shift",description:"Keeps a chosen focal band sharp while blurring its surroundings.",category:"Blur & Glow"},triangleBlur:{label:"Triangle Blur",description:"Blends neighboring pixels with a lightweight triangular falloff.",category:"Blur & Glow"},zoomBlur:{label:"Zoom Blur",description:"Pulls samples toward a focal point for a kinetic rush.",category:"Blur & Glow"},colorHalftone:{label:"Color Halftone",description:"Separates color channels into a rotating print-screen pattern.",category:"Stylize"},dotScreen:{label:"Dot Screen",description:"Transforms luminance into a rotating monochrome print pattern.",category:"Stylize"},edgeWork:{label:"Edge Work",description:"Isolates fine image contours into a high-contrast line drawing.",category:"Stylize"},hexagonalPixelate:{label:"Hexagonal Pixelate",description:"Rebuilds the scene as a mosaic of adjustable hexagonal cells.",category:"Stylize"},ink:{label:"Ink",description:"Extracts local contrast into a graphic hand-inked treatment.",category:"Stylize"},bulgePinch:{label:"Bulge + Pinch",description:"Pushes pixels outward or pulls them inward around a lens center.",category:"Warp & Lens"},magnify:{label:"Magnify",description:"Places an adjustable circular magnifying lens over the image.",category:"Warp & Lens"},swirl:{label:"Swirl",description:"Warps pixels around a configurable lens center.",category:"Warp & Lens"},denoise:{label:"Denoise",description:"Smooths unwanted grain while preserving local image structure.",category:"Finish & Detail"},fxaa:{label:"FXAA",description:"Smooths jagged high-contrast edges with fast approximate antialiasing.",category:"Finish & Detail"},noise:{label:"Noise",description:"Adds adjustable monochrome grain for a tactile film finish.",category:"Finish & Detail"},vignette:{label:"Vignette",description:"Guides attention with a soft radial falloff.",category:"Finish & Detail"}},x={name:"kineticScene",source:"struct kineticSceneUniforms {\n  time: f32,\n  energy: f32,\n};\n\n@group(0) @binding(auto) var<uniform> kineticScene: kineticSceneUniforms;\n\nfn kineticScene_hash21(point: vec2f) -> f32 {\n  return fract(sin(dot(point, vec2f(127.1, 311.7))) * 43758.5453);\n}\n\nfn kineticScene_rotate(point: vec2f, angle: f32) -> vec2f {\n  let sineAngle = sin(angle);\n  let cosineAngle = cos(angle);\n  return mat2x2f(cosineAngle, sineAngle, -sineAngle, cosineAngle) * point;\n}\n\nfn kineticScene_palette(value: f32) -> vec3f {\n  let phase = vec3f(0.04, 0.29, 0.58);\n  return 0.52 + 0.48 * cos(6.2831853 * (value + phase));\n}\n\nfn kineticScene_line(distanceToLine: f32, width: f32, softness: f32) -> f32 {\n  return 1.0 - smoothstep(width, width + softness, abs(distanceToLine));\n}\n\nfn kineticScene_sampleColor(\n  sourceTexture: texture_2d<f32>,\n  sourceTextureSampler: sampler,\n  texSize: vec2f,\n  texCoord: vec2f\n) -> vec4f {\n  let aspect = texSize.x / max(texSize.y, 1.0);\n  // Full-screen texture coordinates use opposite vertical origins in the two backends.\n  let sceneTexCoord = vec2f(texCoord.x, 1.0 - texCoord.y);\n  var point = sceneTexCoord * 2.0 - 1.0;\n  point.x *= aspect;\n\n  let energy = clamp(kineticScene.energy, 0.0, 1.5);\n  let animatedTime = kineticScene.time * (0.32 + energy * 0.24);\n  let radialDistance = length(point);\n\n  let skyAmount = clamp(sceneTexCoord.y, 0.0, 1.0);\n  var color = mix(vec3f(0.006, 0.009, 0.026), vec3f(0.025, 0.035, 0.085), skyAmount);\n  color += vec3f(0.018, 0.008, 0.038) * (1.0 - radialDistance * 0.35);\n\n  // A sparse, slowly drifting star field keeps the upper background alive without adding noise\n  // to filters that react strongly to high-frequency detail.\n  let starPoint = vec2f(point.x + animatedTime * 0.018, point.y) * vec2f(42.0, 30.0);\n  let starCell = floor(starPoint);\n  let starHash = kineticScene_hash21(starCell);\n  let starPosition = fract(starPoint) - 0.5;\n  let starShape = 1.0 - smoothstep(0.018, 0.075, length(starPosition));\n  let starMask = step(0.965, starHash) * smoothstep(-0.05, 0.4, point.y);\n  let starTwinkle = 0.55 + 0.45 * sin(animatedTime * 2.2 + starHash * 38.0);\n  color += vec3f(0.38, 0.62, 1.0) * starShape * starMask * starTwinkle * 0.38;\n\n  // Perspective floor grid. The line spacing tightens towards the horizon, giving blur,\n  // distortion and edge filters useful structured detail to work with.\n  let floorHeight = -(point.y + 0.12);\n  if (floorHeight > 0.0) {\n    let floorDepth = 0.32 / max(floorHeight, 0.018);\n    let floorCoordinate = vec2f(point.x * floorDepth * 1.8, floorDepth + animatedTime * 0.9);\n    let gridCell = abs(fract(floorCoordinate) - 0.5);\n    let gridLines = max(\n      smoothstep(0.465, 0.5, gridCell.x),\n      smoothstep(0.465, 0.5, gridCell.y)\n    );\n    let majorCell = abs(fract(floorCoordinate * 0.2) - 0.5);\n    let majorLines = max(\n      smoothstep(0.475, 0.5, majorCell.x),\n      smoothstep(0.475, 0.5, majorCell.y)\n    );\n    let horizonFade = smoothstep(0.0, 0.16, floorHeight) * exp(-floorDepth * 0.055);\n    color += vec3f(0.02, 0.18, 0.31) * gridLines * horizonFade * 0.48;\n    color += vec3f(0.08, 0.46, 0.62) * majorLines * horizonFade * 0.34;\n  }\n\n  // Two broad chromatic ribbons move behind the central sculpture. Their gradients and clean\n  // edges make color, halftone and warp effects visibly distinct at a glance.\n  let ribbonEnvelope = 1.0 - smoothstep(0.25, 1.65, abs(point.x));\n  let cyanRibbonY = 0.21 * sin(point.x * 2.6 + animatedTime * 1.2) - 0.03;\n  let magentaRibbonY = 0.17 * sin(point.x * 3.1 - animatedTime * 0.86 + 2.1) + 0.08;\n  let cyanRibbon = kineticScene_line(point.y - cyanRibbonY, 0.025, 0.05) * ribbonEnvelope;\n  let magentaRibbon =\n    kineticScene_line(point.y - magentaRibbonY, 0.018, 0.045) * ribbonEnvelope;\n  color += vec3f(0.015, 0.38, 0.52) * cyanRibbon * (0.5 + energy * 0.25);\n  color += vec3f(0.42, 0.04, 0.34) * magentaRibbon * (0.45 + energy * 0.24);\n\n  let orbCenter = vec2f(0.0, 0.09 + sin(animatedTime * 0.75) * 0.018);\n  let orbPoint = point - orbCenter;\n  let orbRadius = 0.255 + sin(animatedTime * 1.35) * 0.008 * energy;\n  let normalizedOrbDistance = length(orbPoint) / orbRadius;\n  let orbMask = 1.0 - smoothstep(0.985, 1.015, normalizedOrbDistance);\n\n  if (normalizedOrbDistance < 1.02) {\n    let sphereZ = sqrt(max(0.0, 1.0 - normalizedOrbDistance * normalizedOrbDistance));\n    let sphereNormal = normalize(vec3f(orbPoint / orbRadius, sphereZ));\n    let lightDirection = normalize(\n      vec3f(cos(animatedTime * 0.85), 0.72, 0.72 + sin(animatedTime * 0.85) * 0.18)\n    );\n    let diffuse = max(dot(sphereNormal, lightDirection), 0.0);\n    let fresnel = pow(1.0 - sphereZ, 2.6);\n    let latitude = sphereNormal.y * 0.34 + animatedTime * 0.11;\n    let orbColor = kineticScene_palette(latitude) * (0.16 + diffuse * 0.52);\n    let innerBand =\n      kineticScene_line(sin((sphereNormal.x + sphereNormal.y) * 15.0 + animatedTime * 2.0), 0.0, 0.22);\n    color = mix(color, orbColor + innerBand * vec3f(0.08, 0.23, 0.34), orbMask);\n    color += vec3f(0.16, 0.56, 0.72) * fresnel * orbMask * (0.42 + energy * 0.3);\n  }\n\n  let orbEdgeDistance = abs(length(orbPoint) - orbRadius);\n  let orbHalo = exp(-orbEdgeDistance * 25.0) * (1.0 - orbMask * 0.6);\n  color += vec3f(0.05, 0.34, 0.55) * orbHalo * (0.38 + energy * 0.34);\n\n  // Elliptical orbital rings turn at different rates and include a dashed technical scale.\n  let ringPointA = kineticScene_rotate(orbPoint, animatedTime * 0.43);\n  let ringPointB = kineticScene_rotate(orbPoint, -animatedTime * 0.31 + 0.8);\n  let ringDistanceA = length(vec2f(ringPointA.x, ringPointA.y * 2.45));\n  let ringDistanceB = length(vec2f(ringPointB.x, ringPointB.y * 1.8));\n  let ringA = kineticScene_line(ringDistanceA - 0.43, 0.003, 0.012);\n  let ringB = kineticScene_line(ringDistanceB - 0.56, 0.003, 0.014);\n  let ringAngle = atan2(ringPointB.y, ringPointB.x);\n  let dash = step(0.38, fract(ringAngle * 2.55 + animatedTime * 0.38));\n  color += vec3f(0.10, 0.64, 0.82) * ringA * (0.42 + energy * 0.25);\n  color += vec3f(0.72, 0.16, 0.58) * ringB * dash * (0.32 + energy * 0.22);\n\n  // Fine radial ticks deliberately retain high-frequency information for denoise, ink and\n  // pixelation effects while fading away from the focal sculpture.\n  let tickAngle = atan2(orbPoint.y, orbPoint.x);\n  let tickPattern = step(0.54, fract(tickAngle * 7.65 + animatedTime * 0.08));\n  let tickRadius = length(orbPoint);\n  let tickBand = (1.0 - smoothstep(0.59, 0.62, tickRadius)) *\n    smoothstep(0.49, 0.52, tickRadius) * tickPattern;\n  color += vec3f(0.16, 0.32, 0.5) * tickBand * 0.45;\n\n  let horizonGlow = exp(-abs(point.y + 0.12) * 24.0);\n  color += vec3f(0.03, 0.16, 0.29) * horizonGlow * (0.36 + energy * 0.18);\n\n  let vignette = clamp(1.12 - dot(point / vec2f(max(aspect, 1.0), 1.0), point / vec2f(max(aspect, 1.0), 1.0)) * 0.3, 0.25, 1.0);\n  color *= vignette;\n\n  // A compact filmic curve guarantees every output channel remains in the SDR-safe 0..1 range.\n  color = vec3f(1.0) - exp(-max(color, vec3f(0.0)) * 1.35);\n  color = pow(clamp(color, vec3f(0.0), vec3f(1.0)), vec3f(0.92));\n  return vec4f(color, 1.0);\n}\n",fs:"layout(std140) uniform kineticSceneUniforms {\n  float time;\n  float energy;\n} kineticScene;\n\nfloat kineticScene_hash21(vec2 point) {\n  return fract(sin(dot(point, vec2(127.1, 311.7))) * 43758.5453);\n}\n\nvec2 kineticScene_rotate(vec2 point, float angle) {\n  float sineAngle = sin(angle);\n  float cosineAngle = cos(angle);\n  return mat2(cosineAngle, sineAngle, -sineAngle, cosineAngle) * point;\n}\n\nvec3 kineticScene_palette(float value) {\n  vec3 phase = vec3(0.04, 0.29, 0.58);\n  return 0.52 + 0.48 * cos(6.2831853 * (value + phase));\n}\n\nfloat kineticScene_line(float distanceToLine, float width, float softness) {\n  return 1.0 - smoothstep(width, width + softness, abs(distanceToLine));\n}\n\nvec4 kineticScene_sampleColor(\n  sampler2D sourceTexture,\n  vec2 texSize,\n  vec2 texCoord\n) {\n  float aspect = texSize.x / max(texSize.y, 1.0);\n  vec2 sceneTexCoord = texCoord;\n  vec2 point = sceneTexCoord * 2.0 - 1.0;\n  point.x *= aspect;\n\n  float energy = clamp(kineticScene.energy, 0.0, 1.5);\n  float animatedTime = kineticScene.time * (0.32 + energy * 0.24);\n  float radialDistance = length(point);\n\n  float skyAmount = clamp(sceneTexCoord.y, 0.0, 1.0);\n  vec3 color = mix(vec3(0.006, 0.009, 0.026), vec3(0.025, 0.035, 0.085), skyAmount);\n  color += vec3(0.018, 0.008, 0.038) * (1.0 - radialDistance * 0.35);\n\n  vec2 starPoint = vec2(point.x + animatedTime * 0.018, point.y) * vec2(42.0, 30.0);\n  vec2 starCell = floor(starPoint);\n  float starHash = kineticScene_hash21(starCell);\n  vec2 starPosition = fract(starPoint) - 0.5;\n  float starShape = 1.0 - smoothstep(0.018, 0.075, length(starPosition));\n  float starMask = step(0.965, starHash) * smoothstep(-0.05, 0.4, point.y);\n  float starTwinkle = 0.55 + 0.45 * sin(animatedTime * 2.2 + starHash * 38.0);\n  color += vec3(0.38, 0.62, 1.0) * starShape * starMask * starTwinkle * 0.38;\n\n  float floorHeight = -(point.y + 0.12);\n  if (floorHeight > 0.0) {\n    float floorDepth = 0.32 / max(floorHeight, 0.018);\n    vec2 floorCoordinate = vec2(point.x * floorDepth * 1.8, floorDepth + animatedTime * 0.9);\n    vec2 gridCell = abs(fract(floorCoordinate) - 0.5);\n    float gridLines = max(\n      smoothstep(0.465, 0.5, gridCell.x),\n      smoothstep(0.465, 0.5, gridCell.y)\n    );\n    vec2 majorCell = abs(fract(floorCoordinate * 0.2) - 0.5);\n    float majorLines = max(\n      smoothstep(0.475, 0.5, majorCell.x),\n      smoothstep(0.475, 0.5, majorCell.y)\n    );\n    float horizonFade = smoothstep(0.0, 0.16, floorHeight) * exp(-floorDepth * 0.055);\n    color += vec3(0.02, 0.18, 0.31) * gridLines * horizonFade * 0.48;\n    color += vec3(0.08, 0.46, 0.62) * majorLines * horizonFade * 0.34;\n  }\n\n  float ribbonEnvelope = 1.0 - smoothstep(0.25, 1.65, abs(point.x));\n  float cyanRibbonY = 0.21 * sin(point.x * 2.6 + animatedTime * 1.2) - 0.03;\n  float magentaRibbonY = 0.17 * sin(point.x * 3.1 - animatedTime * 0.86 + 2.1) + 0.08;\n  float cyanRibbon = kineticScene_line(point.y - cyanRibbonY, 0.025, 0.05) * ribbonEnvelope;\n  float magentaRibbon =\n    kineticScene_line(point.y - magentaRibbonY, 0.018, 0.045) * ribbonEnvelope;\n  color += vec3(0.015, 0.38, 0.52) * cyanRibbon * (0.5 + energy * 0.25);\n  color += vec3(0.42, 0.04, 0.34) * magentaRibbon * (0.45 + energy * 0.24);\n\n  vec2 orbCenter = vec2(0.0, 0.09 + sin(animatedTime * 0.75) * 0.018);\n  vec2 orbPoint = point - orbCenter;\n  float orbRadius = 0.255 + sin(animatedTime * 1.35) * 0.008 * energy;\n  float normalizedOrbDistance = length(orbPoint) / orbRadius;\n  float orbMask = 1.0 - smoothstep(0.985, 1.015, normalizedOrbDistance);\n\n  if (normalizedOrbDistance < 1.02) {\n    float sphereZ = sqrt(max(0.0, 1.0 - normalizedOrbDistance * normalizedOrbDistance));\n    vec3 sphereNormal = normalize(vec3(orbPoint / orbRadius, sphereZ));\n    vec3 lightDirection = normalize(\n      vec3(cos(animatedTime * 0.85), 0.72, 0.72 + sin(animatedTime * 0.85) * 0.18)\n    );\n    float diffuse = max(dot(sphereNormal, lightDirection), 0.0);\n    float fresnel = pow(1.0 - sphereZ, 2.6);\n    float latitude = sphereNormal.y * 0.34 + animatedTime * 0.11;\n    vec3 orbColor = kineticScene_palette(latitude) * (0.16 + diffuse * 0.52);\n    float innerBand =\n      kineticScene_line(sin((sphereNormal.x + sphereNormal.y) * 15.0 + animatedTime * 2.0), 0.0, 0.22);\n    color = mix(color, orbColor + innerBand * vec3(0.08, 0.23, 0.34), orbMask);\n    color += vec3(0.16, 0.56, 0.72) * fresnel * orbMask * (0.42 + energy * 0.3);\n  }\n\n  float orbEdgeDistance = abs(length(orbPoint) - orbRadius);\n  float orbHalo = exp(-orbEdgeDistance * 25.0) * (1.0 - orbMask * 0.6);\n  color += vec3(0.05, 0.34, 0.55) * orbHalo * (0.38 + energy * 0.34);\n\n  vec2 ringPointA = kineticScene_rotate(orbPoint, animatedTime * 0.43);\n  vec2 ringPointB = kineticScene_rotate(orbPoint, -animatedTime * 0.31 + 0.8);\n  float ringDistanceA = length(vec2(ringPointA.x, ringPointA.y * 2.45));\n  float ringDistanceB = length(vec2(ringPointB.x, ringPointB.y * 1.8));\n  float ringA = kineticScene_line(ringDistanceA - 0.43, 0.003, 0.012);\n  float ringB = kineticScene_line(ringDistanceB - 0.56, 0.003, 0.014);\n  float ringAngle = atan(ringPointB.y, ringPointB.x);\n  float dash = step(0.38, fract(ringAngle * 2.55 + animatedTime * 0.38));\n  color += vec3(0.10, 0.64, 0.82) * ringA * (0.42 + energy * 0.25);\n  color += vec3(0.72, 0.16, 0.58) * ringB * dash * (0.32 + energy * 0.22);\n\n  float tickAngle = atan(orbPoint.y, orbPoint.x);\n  float tickPattern = step(0.54, fract(tickAngle * 7.65 + animatedTime * 0.08));\n  float tickRadius = length(orbPoint);\n  float tickBand = (1.0 - smoothstep(0.59, 0.62, tickRadius)) *\n    smoothstep(0.49, 0.52, tickRadius) * tickPattern;\n  color += vec3(0.16, 0.32, 0.5) * tickBand * 0.45;\n\n  float horizonGlow = exp(-abs(point.y + 0.12) * 24.0);\n  color += vec3(0.03, 0.16, 0.29) * horizonGlow * (0.36 + energy * 0.18);\n\n  vec2 vignettePoint = point / vec2(max(aspect, 1.0), 1.0);\n  float vignette = clamp(1.12 - dot(vignettePoint, vignettePoint) * 0.3, 0.25, 1.0);\n  color *= vignette;\n\n  color = vec3(1.0) - exp(-max(color, vec3(0.0)) * 1.35);\n  color = pow(clamp(color, vec3(0.0), vec3(1.0)), vec3(0.92));\n  return vec4(color, 1.0);\n}\n",props:{},uniforms:{},uniformTypes:{time:"f32",energy:"f32"},propTypes:{time:{value:0,private:!0},energy:{value:.9,private:!0}},passes:[{sampler:!0}]};function h(e,t,n){return t in e?Object.defineProperty(e,t,{value:n,enumerable:!0,configurable:!0,writable:!0}):e[t]=n,e}function v(e){for(var t=1;t<arguments.length;t++){var n=null!=arguments[t]?arguments[t]:{},o=Object.keys(n);"function"==typeof Object.getOwnPropertySymbols&&(o=o.concat(Object.getOwnPropertySymbols(n).filter(function(e){return Object.getOwnPropertyDescriptor(n,e).enumerable}))),o.forEach(function(t){h(e,t,n[t])})}return e}let b="Neon Bloom",y=["X","Y","Z","W"],S=new Set(["denoise","edgeWork","gaussianBlur","ink","tiltShift","triangleBlur","zoomBlur"]),C={brightnessContrast:{brightness:.08,contrast:.28},hueSaturation:{hue:-.08,saturation:.32},sepia:{amount:.7},toneMapping:{exposure:1.8,maximumLuminance:1},vibrance:{amount:.45},bloom:{radius:9,threshold:.48,intensity:1.85},gaussianBlur:{radius:10},tiltShift:{start:[.12,.36],end:[.88,.64],blurRadius:18,gradientRadius:130},triangleBlur:{radius:14},zoomBlur:{center:[.5,.5],strength:.16},colorHalftone:{center:[.5,.5],angle:.32,size:5},dotScreen:{center:[.5,.5],angle:.42,size:5},edgeWork:{radius:3},hexagonalPixelate:{center:[.5,.5],scale:12},ink:{strength:.36},bulgePinch:{center:[.5,.5],radius:240,strength:.45},magnify:{screenXY:[.5,.5],radiusPixels:150,zoom:2},swirl:{center:[.5,.5],radius:300,angle:1.05},denoise:{strength:.55},noise:{amount:.2},vignette:{radius:.7,amount:.45}},k={Original:{kicker:"Reference",description:"The animated calibration scene with no image processing.",accent:"linear-gradient(135deg, #38bdf8, #1e293b 55%, #f472b6)",passNames:[],values:{}},"Neon Bloom":{kicker:"Light + Finish",description:"Selective glow and a restrained vignette for luminous depth.",accent:"linear-gradient(135deg, #22d3ee, #6366f1 50%, #f472b6)",passNames:["bloom","vignette"],values:{bloom:{radius:9,threshold:.48,intensity:1.85},vignette:{radius:.72,amount:.3}}},"Chromatic Grade":{kicker:"Color Pipeline",description:"A cool hue rotation, selective vibrance, and cinematic contrast.",accent:"linear-gradient(135deg, #34d399, #0891b2 48%, #818cf8)",passNames:["hueSaturation","vibrance","brightnessContrast"],values:{hueSaturation:{hue:-.06,saturation:.3},vibrance:{amount:.42},brightnessContrast:{brightness:-.03,contrast:.18}}},"Graphic Ink":{kicker:"Edge Stylization",description:"Punchy tonal separation with fine illustrated edge work.",accent:"linear-gradient(135deg, #f8fafc, #64748b 45%, #0f172a)",passNames:["brightnessContrast","ink"],values:{brightnessContrast:{brightness:.04,contrast:.28},ink:{strength:.36}}},"Chromatic Print":{kicker:"Print Simulation",description:"Animated RGB geometry resolved through an offset halftone screen.",accent:"linear-gradient(135deg, #f43f5e, #facc15 48%, #22d3ee)",passNames:["colorHalftone","vibrance"],values:{colorHalftone:{center:[.5,.5],angle:.32,size:5.5},vibrance:{amount:.38}}},"Dream Zoom":{kicker:"Motion Optics",description:"A gentle radial pull that preserves the center calibration target.",accent:"linear-gradient(135deg, #a78bfa, #ec4899 52%, #fb7185)",passNames:["zoomBlur","vignette"],values:{zoomBlur:{center:[.5,.5],strength:.12},vignette:{radius:.68,amount:.38}}},"Prism Lens":{kicker:"Pixel Warp",description:"A broad optical swirl followed by highlight recovery.",accent:"linear-gradient(135deg, #60a5fa, #c084fc 48%, #f0abfc)",passNames:["swirl","bloom"],values:{swirl:{center:[.5,.5],radius:360,angle:.75},bloom:{radius:5,threshold:.64,intensity:1.2}}}},z=["Original","Neon Bloom","Chromatic Grade","Graphic Ink","Chromatic Print","Dream Zoom","Prism Lens"];class w extends o.J{onFinalize(){var e;this.settingsPanel.finalize(),this.panels.finalize(),this.sourceTexture.destroy(),null==(e=this.shaderPassRenderer)||e.destroy(),this.originalShaderPassRenderer.destroy()}onRender(e){let{time:t}=e;if(this.previousFrameTime>0&&this.motionEnabled){let e=Math.min(Math.max((t-this.previousFrameTime)/1e3,0),.05);this.sceneTime+=e*this.motionSpeed}this.previousFrameTime=t;let[n,o]=this.device.getCanvasContext().getDrawingBufferSize(),r=B(this.activePassNames);this.shaderPassRenderer.resize([Math.max(Math.round(n*r),1),Math.max(Math.round(o*r),1)]),this.originalShaderPassRenderer.resize([n,o]),(this.comparisonOriginal?this.originalShaderPassRenderer:this.shaderPassRenderer).renderToScreen({sourceTexture:this.sourceTexture,uniforms:this.makeActiveUniforms()})}applyLook(e){let t=arguments.length>1&&void 0!==arguments[1]&&arguments[1],n=k[e];this.selectedLookName=e,this.activePassNames=[...n.passNames],this.applyLookValues(n),this.comparisonOriginal=!1,this.setShaderPasses(this.getActiveShaderPasses()),this.syncPanels(t?'[data-look-name="'.concat(e,'"]'):void 0)}setEffectEnabled(e,t){if(!this.shaderPassMap[e])return;let n=A(this.activePassNames,e,t);this.setActivePassNames(n,'[data-effect-library-item][data-effect-name="'.concat(e,'"]'))}moveEffect(e,t){let n=R(this.activePassNames,e,t),o=t<0?"move-up":"move-down";this.setActivePassNames(n,'[data-effect-action="'.concat(o,'"][data-effect-name="').concat(e,'"]'))}setComparisonOriginal(e){if(0===this.activePassNames.length){this.comparisonOriginal=!1;return}e!==this.comparisonOriginal&&(this.comparisonOriginal=e)}setShaderPasses(e){var t;null==(t=this.shaderPassRenderer)||t.destroy(),this.shaderPassRenderer=new r.y(this.device,{shaderPasses:e})}setActivePassNames(e,t){e!==this.activePassNames&&(this.activePassNames=e,0===this.activePassNames.length&&(this.comparisonOriginal=!1),this.setShaderPasses(this.getActiveShaderPasses()),this.syncPanels(t))}getActiveShaderPasses(){return[x,...this.activePassNames.map(e=>this.shaderPassMap[e]).filter(e=>!!e)]}makeActiveUniforms(){let e={[x.name]:{time:this.sceneTime,energy:this.sceneEnergy}};for(let t of this.activePassNames){let n=this.shaderPassMap[t];n&&(e[n.name]=this.effectValuesByName[t]||{})}return e}applyLookValues(e){for(let t of e.passNames){let n=T(this.shaderPassMap[t]),o=e.values[t]||{};this.effectValuesByName[t]=v({},n,H(o))}}makeTuneSettingsState(){let e={motionEnabled:this.motionEnabled,motionSpeed:this.motionSpeed,sceneEnergy:this.sceneEnergy};for(let t of this.activePassNames)Object.assign(e,N(t,this.effectValuesByName[t]||{}));return e}makeTuneSettingsSchema(){return{title:"Controls",sections:[{id:"source-motion",name:"Animated Source",description:"Control the procedural calibration scene before processing.",initiallyCollapsed:!1,settings:[{name:"motionEnabled",label:"Motion",type:"boolean",persist:"none"},{name:"motionSpeed",label:"Motion Speed",type:"number",persist:"none",min:.15,max:1.6,step:.05},{name:"sceneEnergy",label:"Scene Energy",type:"number",persist:"none",min:.5,max:1.35,step:.01}]},...this.activePassNames.flatMap((e,t)=>{var n,o;let r=this.shaderPassMap[e];if(!r)return[];let a=m[e];return[{id:"effect-".concat(e),name:(null==a?void 0:a.label)||I(e),description:null==a?void 0:a.description,initiallyCollapsed:t>0,settings:(n=e,o=this.effectValuesByName[e]||{},P(r).flatMap(e=>{let[t,r]=e,a=o[t];return"number"==typeof a?[E(n,t,a,r)]:Array.isArray(a)?a.map((e,o)=>E(n,t,e,r,o)):[]}))}]})]}}makePanel(){var e,t,n,o,r,i;let s,c,l,f,u,d,x;return(0,a.lM)({id:"postprocessing-tabs",title:"Effects: Image Processing",tabListLayout:"scroll",panels:[(0,a.TD)({id:"postprocessing-looks",title:"Looks",html:(e=this.selectedLookName,t=this.comparisonOriginal,s=(n=this.activePassNames).length>0,c=L(e,n),l=z.map(t=>{let n=k[t],o=0===n.passNames.length?"Source only":"".concat(n.passNames.length," ").concat(1===n.passNames.length?"pass":"passes");return'\n      <button class="effects-look-card" type="button" data-look-name="'.concat(t,'" aria-pressed="').concat(t===e,'">\n        <span class="effects-look-swatch" style="background:').concat(n.accent,'"></span>\n        <span class="effects-look-copy">\n          <span class="effects-look-kicker">').concat(n.kicker," \xb7 ").concat(o,'</span>\n          <span class="effects-look-title">').concat(t,'</span>\n          <span class="effects-look-description">').concat(n.description,'</span>\n        </span>\n        <span class="effects-look-check" aria-hidden="true">\u2713</span>\n      </button>')}).join(""),'\n    <style>\n      .effects-lab { container-type: inline-size; color: #e2e8f0; font: 13px/1.45 Inter, ui-sans-serif, system-ui, sans-serif; }\n      .effects-lab * { box-sizing: border-box; }\n      .effects-lab-header { padding: 15px 15px 12px; border-bottom: 1px solid rgba(148, 163, 184, .16); background: radial-gradient(circle at 12% 0%, rgba(56, 189, 248, .13), transparent 42%); }\n      .effects-lab-kicker { margin: 0 0 4px; color: #67e8f9; font-size: 10px; font-weight: 800; letter-spacing: .14em; text-transform: uppercase; }\n      .effects-lab-title { margin: 0; color: #f8fafc; font-size: 17px; font-weight: 750; letter-spacing: -.02em; }\n      .effects-lab-intro { margin: 5px 0 0; color: #94a3b8; font-size: 12px; }\n      .effects-lab-chips { display: flex; flex-wrap: wrap; gap: 5px; margin-top: 10px; }\n      .effects-lab-chip { padding: 3px 7px; border: 1px solid rgba(125, 211, 252, .18); border-radius: 999px; background: rgba(14, 116, 144, .1); color: #bae6fd; font-size: 10px; font-weight: 650; }\n      .effects-look-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 8px; padding: 10px; }\n      .effects-look-card { position: relative; display: flex; min-width: 0; min-height: 112px; margin: 0; padding: 0; overflow: hidden; border: 1px solid rgba(148, 163, 184, .16); border-radius: 10px; background: rgba(15, 23, 42, .62); color: inherit; text-align: left; cursor: pointer; transition: transform 120ms ease, border-color 120ms ease, background 120ms ease; }\n      .effects-look-card:hover { transform: translateY(-1px); border-color: rgba(125, 211, 252, .46); background: rgba(30, 41, 59, .78); }\n      .effects-look-card[aria-pressed=\'true\'] { border-color: rgba(103, 232, 249, .68); background: linear-gradient(145deg, rgba(8, 47, 73, .7), rgba(30, 41, 59, .82)); box-shadow: inset 0 0 0 1px rgba(103, 232, 249, .12), 0 8px 22px rgba(2, 8, 23, .22); }\n      .effects-look-swatch { flex: 0 0 7px; align-self: stretch; opacity: .9; }\n      .effects-look-copy { display: flex; min-width: 0; flex-direction: column; padding: 10px 9px 10px 10px; }\n      .effects-look-kicker { color: #7dd3fc; font-size: 9px; font-weight: 800; letter-spacing: .07em; text-transform: uppercase; }\n      .effects-look-title { margin-top: 2px; color: #f8fafc; font-size: 12px; font-weight: 750; }\n      .effects-look-description { margin-top: 4px; color: #94a3b8; font-size: 10.5px; line-height: 1.35; }\n      .effects-look-check { position: absolute; top: 7px; right: 7px; display: none; color: #67e8f9; font-size: 11px; font-weight: 900; }\n      .effects-look-card[aria-pressed=\'true\'] .effects-look-check { display: block; }\n      .effects-lab-actions { display: flex; flex-wrap: wrap; gap: 7px; padding: 0 10px 11px; }\n      .effects-action { min-height: 35px; margin: 0; padding: 7px 9px; border: 1px solid rgba(148, 163, 184, .2); border-radius: 8px; background: rgba(15, 23, 42, .72); color: #cbd5e1; font: 650 11px/1.2 Inter, ui-sans-serif, system-ui, sans-serif; cursor: pointer; }\n      .effects-action:hover, .effects-action:focus-visible { border-color: rgba(125, 211, 252, .55); color: #f8fafc; outline: none; }\n      .effects-action[aria-pressed=\'true\'] { border-color: #67e8f9; background: rgba(8, 145, 178, .22); color: #ecfeff; }\n      .effects-action:disabled { cursor: not-allowed; opacity: .42; }\n      .effects-action-primary { border-color: rgba(103, 232, 249, .4); background: rgba(8, 47, 73, .7); color: #cffafe; }\n      .effects-look-customized { border-color: rgba(251, 191, 36, .3); background: rgba(120, 53, 15, .24); color: #fde68a; }\n      .effects-lab-hint { margin: 0; padding: 0 12px 13px; color: #64748b; font-size: 10px; text-align: center; }\n      @container (max-width: 330px) { .effects-look-grid { grid-template-columns: 1fr; } }\n    </style>\n    <div class="effects-lab">\n      <header class="effects-lab-header">\n        <p class="effects-lab-kicker">Realtime image laboratory</p>\n        <h3 class="effects-lab-title">Kinetic Color Lab</h3>\n        <p class="effects-lab-intro">Start with a look, customize its effect stack, and adjust every live uniform.</p>\n        <div class="effects-lab-chips"><span class="effects-lab-chip">'.concat(Object.keys(g).length,' image effects</span><span class="effects-lab-chip">WebGL + WebGPU</span>').concat(c?'<span class="effects-lab-chip effects-look-customized">Customized stack</span>':"",'</div>\n      </header>\n      <div class="effects-look-grid">').concat(l,'</div>\n      <div class="effects-lab-actions">\n        <button class="effects-action effects-action-primary" type="button" data-open-effects-stack>Customize this look</button>\n        <button class="effects-action" type="button" data-original-compare aria-pressed="').concat(t,'" ').concat(s?"":"disabled",'>Original preview</button>\n      </div>\n      <p class="effects-lab-hint">Effects Stack adds or reorders passes. Controls adjusts their live settings.</p>\n    </div>')),onRender:e=>this.bindLooksPanel(e)}),(0,a.TD)({id:"postprocessing-stack",title:"Effects Stack",html:(o=this.activePassNames,r=this.selectedLookName,i=this.effectSearchQuery,f=L(r,o),u=Object.keys(g).length,d=o.map((e,t)=>{let n=m[e],r=(null==n?void 0:n.label)||I(e);return'\n        <li class="effects-stack-step">\n          <span class="effects-stack-step-number" aria-hidden="true">'.concat(String(t+1).padStart(2,"0"),'</span>\n          <span class="effects-stack-step-copy">\n            <span class="effects-stack-step-title">').concat(j(r),'</span>\n            <span class="effects-stack-step-category">').concat(j((null==n?void 0:n.category)||"Image effect"),'</span>\n          </span>\n          <span class="effects-stack-step-actions">\n            <button class="effects-stack-icon-button" type="button" data-effect-action="move-up" data-effect-name="').concat(e,'" aria-label="Move ').concat(j(r),' earlier" ').concat(0===t?"disabled":"",'>\u2191</button>\n            <button class="effects-stack-icon-button" type="button" data-effect-action="move-down" data-effect-name="').concat(e,'" aria-label="Move ').concat(j(r),' later" ').concat(t===o.length-1?"disabled":"",'>\u2193</button>\n            <button class="effects-stack-icon-button effects-stack-remove" type="button" data-effect-action="remove" data-effect-name="').concat(e,'" aria-label="Remove ').concat(j(r),'">\xd7</button>\n          </span>\n        </li>')}).join(""),x=p.map(e=>{let t=Object.keys(g).filter(t=>{var n;return(null==(n=m[t])?void 0:n.category)===e}),n=t.map(e=>{let t=m[e],n=o.includes(e),r=n?"remove":"add",a="".concat(t.label," ").concat(t.category," ").concat(t.description);return'\n          <button class="effects-library-item" type="button" data-effect-library-item data-effect-action="'.concat(r,'" data-effect-name="').concat(e,'" data-effect-search-text="').concat(j(a.toLowerCase()),'" aria-label="').concat(n?"Remove":"Add"," ").concat(j(t.label),'" aria-pressed="').concat(n,'">\n            <span class="effects-library-copy">\n              <span class="effects-library-title">').concat(j(t.label),'</span>\n              <span class="effects-library-description">').concat(j(t.description),'</span>\n            </span>\n            <span class="effects-library-action" aria-hidden="true">').concat(n?"\u2713":"+","</span>\n          </button>")}).join("");return'\n      <section class="effects-library-group" data-effect-category-group>\n        <h4 class="effects-library-group-title">'.concat(j(e)," <span>").concat(t.length,'</span></h4>\n        <div class="effects-library-list">').concat(n,"</div>\n      </section>")}).join(""),'\n    <style>\n      .effects-stack { container-type: inline-size; color: #e2e8f0; font: 12px/1.45 Inter, ui-sans-serif, system-ui, sans-serif; }\n      .effects-stack * { box-sizing: border-box; }\n      .effects-stack [hidden] { display: none !important; }\n      .effects-stack-header { padding: 15px 15px 12px; border-bottom: 1px solid rgba(148, 163, 184, .16); background: radial-gradient(circle at 12% 0%, rgba(56, 189, 248, .13), transparent 46%); }\n      .effects-stack-kicker { margin: 0 0 4px; color: #67e8f9; font-size: 10px; font-weight: 800; letter-spacing: .14em; text-transform: uppercase; }\n      .effects-stack-title { margin: 0; color: #f8fafc; font-size: 17px; font-weight: 750; letter-spacing: -.02em; }\n      .effects-stack-intro { margin: 5px 0 0; color: #94a3b8; font-size: 11px; }\n      .effects-stack-origin { display: inline-flex; margin-top: 9px; padding: 3px 8px; border: 1px solid rgba(125, 211, 252, .18); border-radius: 999px; background: rgba(14, 116, 144, .1); color: #bae6fd; font-size: 10px; font-weight: 650; }\n      .effects-stack-origin-custom { border-color: rgba(251, 191, 36, .3); background: rgba(120, 53, 15, .22); color: #fde68a; }\n      .effects-stack-section { padding: 12px 12px 0; }\n      .effects-stack-section-heading { display: flex; align-items: center; justify-content: space-between; gap: 10px; margin-bottom: 9px; }\n      .effects-stack-section-title { margin: 0; color: #f8fafc; font-size: 11px; font-weight: 750; letter-spacing: .04em; text-transform: uppercase; }\n      .effects-stack-count { color: #7dd3fc; font-size: 10px; font-weight: 700; }\n      .effects-stack-source { display: flex; align-items: center; gap: 8px; min-height: 33px; padding: 7px 9px; border: 1px solid rgba(56, 189, 248, .24); border-radius: 8px; background: rgba(8, 47, 73, .48); color: #bae6fd; font-size: 10px; font-weight: 650; }\n      .effects-stack-source-dot { width: 7px; height: 7px; flex: 0 0 7px; border-radius: 50%; background: #67e8f9; box-shadow: 0 0 10px rgba(103, 232, 249, .7); }\n      .effects-stack-list { display: grid; gap: 6px; margin: 7px 0 0; padding: 0; list-style: none; }\n      .effects-stack-step { display: flex; align-items: center; min-height: 49px; gap: 8px; padding: 7px; border: 1px solid rgba(148, 163, 184, .17); border-radius: 8px; background: rgba(15, 23, 42, .66); }\n      .effects-stack-step-number { width: 20px; color: #64748b; font-size: 10px; font-weight: 800; text-align: center; }\n      .effects-stack-step-copy { display: flex; min-width: 0; flex: 1 1 auto; flex-direction: column; }\n      .effects-stack-step-title { overflow: hidden; color: #f8fafc; font-size: 11px; font-weight: 700; text-overflow: ellipsis; white-space: nowrap; }\n      .effects-stack-step-category { overflow: hidden; color: #94a3b8; font-size: 9px; text-overflow: ellipsis; white-space: nowrap; }\n      .effects-stack-step-actions { display: flex; flex: 0 0 auto; gap: 4px; }\n      .effects-stack-icon-button { display: inline-flex; width: 26px; height: 26px; align-items: center; justify-content: center; margin: 0; padding: 0; border: 1px solid rgba(148, 163, 184, .2); border-radius: 6px; background: rgba(30, 41, 59, .68); color: #cbd5e1; font-size: 14px; cursor: pointer; }\n      .effects-stack-icon-button:hover, .effects-stack-icon-button:focus-visible { border-color: rgba(125, 211, 252, .58); color: #f8fafc; outline: none; }\n      .effects-stack-icon-button:disabled { cursor: not-allowed; opacity: .32; }\n      .effects-stack-remove:hover, .effects-stack-remove:focus-visible { border-color: rgba(251, 113, 133, .62); color: #fecdd3; }\n      .effects-stack-empty { margin: 9px 0 0; padding: 12px; border: 1px dashed rgba(148, 163, 184, .24); border-radius: 8px; color: #94a3b8; font-size: 10px; text-align: center; }\n      .effects-library { margin-top: 16px; padding-bottom: 13px; }\n      .effects-library-search { display: block; width: 100%; min-height: 37px; margin: 0 0 9px; padding: 8px 10px; border: 1px solid rgba(148, 163, 184, .24); border-radius: 8px; background: rgba(15, 23, 42, .7); color: #f8fafc; font: 11px/1.2 Inter, ui-sans-serif, system-ui, sans-serif; }\n      .effects-library-search::placeholder { color: #64748b; }\n      .effects-library-search:focus { border-color: rgba(125, 211, 252, .62); outline: none; }\n      .effects-library-group { margin-top: 12px; }\n      .effects-library-group-title { display: flex; align-items: center; justify-content: space-between; margin: 0 0 6px; color: #cbd5e1; font-size: 10px; font-weight: 750; letter-spacing: .04em; text-transform: uppercase; }\n      .effects-library-group-title span { color: #64748b; font-size: 9px; }\n      .effects-library-list { display: grid; gap: 5px; }\n      .effects-library-item { display: flex; width: 100%; align-items: center; justify-content: space-between; gap: 8px; min-height: 49px; margin: 0; padding: 8px 9px; border: 1px solid rgba(148, 163, 184, .14); border-radius: 8px; background: rgba(15, 23, 42, .55); color: inherit; text-align: left; cursor: pointer; }\n      .effects-library-item:hover, .effects-library-item:focus-visible { border-color: rgba(125, 211, 252, .48); background: rgba(30, 41, 59, .72); outline: none; }\n      .effects-library-item[aria-pressed=\'true\'] { border-color: rgba(103, 232, 249, .45); background: rgba(8, 47, 73, .58); }\n      .effects-library-copy { display: flex; min-width: 0; flex: 1 1 auto; flex-direction: column; gap: 2px; }\n      .effects-library-title { color: #f8fafc; font-size: 10px; font-weight: 700; }\n      .effects-library-description { color: #94a3b8; font-size: 9px; line-height: 1.35; }\n      .effects-library-action { display: inline-flex; width: 22px; height: 22px; flex: 0 0 22px; align-items: center; justify-content: center; border: 1px solid rgba(148, 163, 184, .25); border-radius: 6px; color: #7dd3fc; font-size: 15px; }\n      .effects-library-item[aria-pressed=\'true\'] .effects-library-action { border-color: rgba(103, 232, 249, .42); background: rgba(8, 145, 178, .22); color: #67e8f9; }\n      .effects-library-no-results { margin: 10px 0 0; color: #94a3b8; font-size: 10px; text-align: center; }\n      @container (max-width: 300px) { .effects-stack-step { gap: 5px; padding: 6px; } .effects-stack-step-number { width: 16px; } .effects-stack-icon-button { width: 24px; height: 24px; } }\n    </style>\n    <section class="effects-stack">\n      <header class="effects-stack-header">\n        <p class="effects-stack-kicker">Build your own pipeline</p>\n        <h3 class="effects-stack-title">Effect Composer</h3>\n        <p class="effects-stack-intro">Add any image effect, reorder the live stack, and fine-tune each pass under Controls.</p>\n        <span class="effects-stack-origin'.concat(f?" effects-stack-origin-custom":"",'">').concat(j(r)).concat(f?" \xb7 customized":" preset",'</span>\n      </header>\n      <section class="effects-stack-section" aria-label="Active effect stack">\n        <div class="effects-stack-section-heading">\n          <h4 class="effects-stack-section-title">Active pipeline</h4>\n          <span class="effects-stack-count" aria-live="polite">').concat(o.length," ").concat(1===o.length?"pass":"passes",'</span>\n        </div>\n        <div class="effects-stack-source"><span class="effects-stack-source-dot" aria-hidden="true"></span>Animated source \xb7 always on</div>\n        ').concat(d?'<ol class="effects-stack-list">'.concat(d,"</ol>"):'<p class="effects-stack-empty">No effects yet. Add any module from the library below.</p>','\n      </section>\n      <section class="effects-stack-section effects-library" aria-label="Available image effects">\n        <div class="effects-stack-section-heading">\n          <h4 class="effects-stack-section-title">Effect library</h4>\n          <span class="effects-stack-count" data-effect-result-count>').concat(u,' effects</span>\n        </div>\n        <input class="effects-library-search" type="search" data-effect-search aria-label="Search available effects" placeholder="Search ').concat(u,' effects" value="').concat(j(i),'" />\n        ').concat(x,'\n        <p class="effects-library-no-results" data-effect-no-results hidden>No matching effects. Try a different search.</p>\n      </section>\n    </section>')),onRender:e=>this.bindEffectsStackPanel(e)}),this.settingsPanel.makePanel(),(0,a.TD)({id:"postprocessing-about",title:"About",html:'\n    <div style="padding:14px 15px;color:#cbd5e1;font:12px/1.55 Inter,ui-sans-serif,system-ui,sans-serif">\n      <p style="margin:0 0 10px;color:#f8fafc;font-size:15px;font-weight:750">A live test chart, not a fixed photograph.</p>\n      <p style="margin:0 0 10px">The first fullscreen pass generates a moving scene with fine lines, broad gradients, saturated edges, highlights, and low-contrast shadow detail. Those signals make blur, bloom, color, edge, print, and warp behavior legible at a glance.</p>\n      <p style="margin:0 0 10px"><b style="color:#7dd3fc">Editable pipelines:</b> each Look starts with ordinary <code>ShaderPass</code> modules. Effects Stack exposes the complete portable image-effect library, and Controls adjusts the same typed properties an application can drive directly. Sample-heavy stacks use a controlled processing resolution to stay fluid on high-DPI displays.</p>\n      <p style="margin:0"><b style="color:#7dd3fc">Portable by design:</b> the procedural source and every selected effect provide both WGSL and GLSL, so the visual language stays consistent across WebGPU and WebGL.</p>\n    </div>'})]})}bindLooksPanel(e){let t=e=>{var t;if(!(e.target instanceof Element))return;let n=e.target.closest("[data-look-name]"),o=null==n?void 0:n.dataset.lookName;if((t=o)&&t in k)return void this.applyLook(o,!0);if(e.target.closest("[data-open-effects-stack]"))return void this.openPanelTab("Effects Stack");let r=e.target.closest("[data-original-compare]");r&&!r.disabled&&(this.setComparisonOriginal(!this.comparisonOriginal),r.setAttribute("aria-pressed",String(this.comparisonOriginal)))};return e.addEventListener("click",t),()=>{e.removeEventListener("click",t)}}bindEffectsStackPanel(e){let t=e=>{if(!(e.target instanceof Element))return;let t=e.target.closest("[data-effect-action]"),n=null==t?void 0:t.dataset.effectName;if(n&&this.shaderPassMap[n])switch(null==t?void 0:t.dataset.effectAction){case"add":this.setEffectEnabled(n,!0);break;case"remove":this.setEffectEnabled(n,!1);break;case"move-up":this.moveEffect(n,-1);break;case"move-down":this.moveEffect(n,1)}},n=t=>{t.target instanceof HTMLInputElement&&t.target.hasAttribute("data-effect-search")&&(this.effectSearchQuery=t.target.value,M(e,this.effectSearchQuery))};return e.addEventListener("click",t),e.addEventListener("input",n),M(e,this.effectSearchQuery),()=>{e.removeEventListener("click",t),e.removeEventListener("input",n)}}openPanelTab(e){let t=Array.from(document.querySelectorAll("[data-panel-tabs] > button")).find(t=>{var n;return(null==(n=t.textContent)?void 0:n.trim())===e});t&&(t.dispatchEvent(new Event("pointerdown",{bubbles:!0,cancelable:!0})),t.focus())}syncPanels(e){this.settingsPanel.setSchemaAndSettings(this.makeTuneSettingsSchema(),this.makeTuneSettingsState()),this.panels.setPanel(this.makePanel()),e&&"u">typeof window&&window.requestAnimationFrame(()=>{let t=document.querySelector(e);t&&!t.disabled&&t.focus()})}constructor({device:e}){super(),h(this,"device",void 0),h(this,"sourceTexture",void 0),h(this,"shaderPassMap",void 0),h(this,"settingsPanel",void 0),h(this,"panels",void 0),h(this,"originalShaderPassRenderer",void 0),h(this,"effectValuesByName",void 0),h(this,"shaderPassRenderer",void 0),h(this,"selectedLookName",b),h(this,"activePassNames",[]),h(this,"motionEnabled",!0),h(this,"motionSpeed",.72),h(this,"sceneEnergy",1),h(this,"sceneTime",0),h(this,"previousFrameTime",0),h(this,"comparisonOriginal",!1),h(this,"effectSearchQuery",""),h(this,"handleSettingsChange",(e,t)=>{var n,o,r;let i=null==(n=(0,a.ZT)(t,"motionEnabled"))?void 0:n.nextValue,s=null==(o=(0,a.ZT)(t,"motionSpeed"))?void 0:o.nextValue,c=null==(r=(0,a.ZT)(t,"sceneEnergy"))?void 0:r.nextValue;for(let t of("boolean"==typeof i&&(this.motionEnabled=i),"number"==typeof s&&(this.motionSpeed=s),"number"==typeof c&&(this.sceneEnergy=c),this.activePassNames))this.effectValuesByName[t]=W(t,e,this.effectValuesByName[t]||{})}),this.device=e,this.sourceTexture=e.createTexture({id:"effects-image-processing-seed",data:new Uint8Array([2,4,12,255]),width:1,height:1,format:"rgba8unorm"}),this.shaderPassMap=v({},g),this.effectValuesByName=function(e){let t={};for(let[n,o]of Object.entries(e))t[n]=T(o);return t}(this.shaderPassMap),this.applyLookValues(k[this.selectedLookName]);let t=this.constructor.initialEffectName;this.activePassNames=_(t),t&&this.activePassNames[0]===t&&(this.effectValuesByName[t]=v({},this.effectValuesByName[t],H(C[t]||{}))),this.settingsPanel=new a.$9({id:"postprocessing-tune",label:"Controls",sectionPresentation:"accordion",schema:this.makeTuneSettingsSchema(),settings:this.makeTuneSettingsState(),onSettingsChange:this.handleSettingsChange}),this.panels=new a.c1({panel:this.makePanel()}),this.panels.mount(),this.originalShaderPassRenderer=new r.y(this.device,{shaderPasses:[x]}),this.setShaderPasses(this.getActiveShaderPasses())}}function T(e){let t={};if(!e)return t;for(let[o,r]of P(e)){var n;let e="number"==typeof(n=r.value)?n:Array.isArray(n)?[...n]:void 0;void 0!==e&&(t[o]=e)}return t}function P(e){let t=[];for(let[n,o]of Object.entries(e.propTypes||{})){let e="number"==typeof o?{value:o}:o;e.private||void 0===e.value||t.push([n,e])}return t}function _(e){return e&&Object.hasOwn(g,e)?[e]:[...k[b].passNames]}function B(e){return e.includes("zoomBlur")?.65:e.some(e=>S.has(e))?.75:1}function A(e,t,n){let o=e.indexOf(t);return n?o<0?[...e,t]:e:o<0?e:e.filter(e=>e!==t)}function R(e,t,n){let o=e.indexOf(t),r=o+n;if(o<0||r<0||r>=e.length)return e;let a=[...e];return[a[o],a[r]]=[a[r],a[o]],a}function D(e,t){return v({effectName:e},N(e,t[e]||{}))}function U(e,t,n){if("No effect"===e)return;let o=n[e];if(o)return{[o.name]:t[e]||{}}}function N(e,t){let n={};for(let[o,r]of Object.entries(t)){if("number"==typeof r){n[O(e,o)]=r;continue}for(let[t,a]of r.entries())n[O(e,o,t)]=a}return n}function W(e,t,n){let o={};for(let[r,a]of Object.entries(n)){if("number"==typeof a){let n=t[O(e,r)];o[r]="number"==typeof n?n:a;continue}o[r]=a.map((n,o)=>{let a=t[O(e,r,o)];return"number"==typeof a?a:n})}return o}function E(e,t,n,o,r){var a,i,s,c,l,f;let u,d,p,g=(a=n,u=null!=(s=null!=(c=(i=o).min)?c:i.softMin)?s:a>=0&&a<=1?0:Math.min(a,0),d=null!=(l=null!=(f=i.max)?f:i.softMax)?l:a>=0&&a<=1?1:Math.max(2*Math.abs(a),u+1),p=Number.isInteger(a)&&Number.isInteger(u)&&Number.isInteger(d)?1:Math.max(Number(((d-u)/200).toFixed(4)),.001),{min:u,max:d,step:p}),m=void 0===r?"":" ".concat(y[r]||"Value ".concat(r+1));return{name:O(e,t,r),label:"".concat(I(t)).concat(m),type:"number",persist:"none",min:g.min,max:g.max,step:g.step}}function O(e,t,n){return void 0===n?"".concat(e,"__").concat(t):"".concat(e,"__").concat(t,"__").concat(n)}function M(e,t){let n=t.trim().toLowerCase(),o=e.querySelectorAll("[data-effect-library-item]"),r=0;for(let e of o){let t=e.dataset.effectSearchText||"",o=!n||t.includes(n);e.hidden=!o,o&&r++}for(let t of e.querySelectorAll("[data-effect-category-group]"))t.hidden=!t.querySelector("[data-effect-library-item]:not([hidden])");let a=e.querySelector("[data-effect-result-count]");a&&(a.textContent="".concat(r," ").concat(1===r?"effect":"effects"));let i=e.querySelector("[data-effect-no-results]");i&&(i.hidden=r>0)}function L(e,t){let n=k[e].passNames;return n.length!==t.length||n.some((e,n)=>e!==t[n])}function j(e){return e.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;")}function H(e){return Object.fromEntries(Object.entries(e).map(e=>{let[t,n]=e;return[t,"number"==typeof n?n:[...n]]}))}function I(e){return e.replace(/([A-Z])/g," $1").replace(/^./,e=>e.toUpperCase())}h(w,"info",(0,a.S8)()),h(w,"initialEffectName",void 0)},76282(e,t,n){n.d(t,{H9:()=>o});function o(e){return ArrayBuffer.isView(e)&&!(e instanceof DataView)||!!Array.isArray(e)&&(0===e.length||"number"==typeof e[0])}}}]);