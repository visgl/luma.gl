"use strict";(self.webpackChunkwebsite_docusaurus=self.webpackChunkwebsite_docusaurus||[]).push([["66350"],{29885(e,n,t){t.r(n),t.d(n,{default:()=>j});var i=t(68523),o=t(61601),a=t(27867),r=t(72831),l=t(54064),c=t(78503),s=t(42250),d=t(5637),p=t(76040),u=t(36029),m=t(33937);let g={time:0,baseColor:[.04,.18,.31],opacity:.82,fresnelColor:[.86,.95,1],fresnelPower:5,specularIntensity:1.4,normalStrength:.35,mappingMode:0,coordinateScale:[1,1],coordinateOffset:[0,0],waveADirection:[.9805806756909201,.19611613513818402],waveASpeed:.6,waveAFrequency:4,waveAAmplitude:.08,waveBDirection:[.09950371902099893,.9950371902099893],waveBSpeed:-.45,waveBFrequency:7,waveBAmplitude:.04},f={name:"waterMaterial",firstBindingSlot:0,bindingLayout:[{name:"waterMaterial",group:3}],dependencies:[m.x],source:"struct waterMaterialUniforms {\n  time: f32,\n  baseColor: vec3<f32>,\n  opacity: f32,\n  fresnelColor: vec3<f32>,\n  fresnelPower: f32,\n  specularIntensity: f32,\n  normalStrength: f32,\n  mappingMode: i32,\n  coordinateScale: vec2<f32>,\n  coordinateOffset: vec2<f32>,\n  waveADirection: vec2<f32>,\n  waveASpeed: f32,\n  waveAFrequency: f32,\n  waveAAmplitude: f32,\n  waveBDirection: vec2<f32>,\n  waveBSpeed: f32,\n  waveBFrequency: f32,\n  waveBAmplitude: f32,\n};\n\n@group(3) @binding(auto) var<uniform> waterMaterial : waterMaterialUniforms;\n\nfn water_getDirection(direction: vec2<f32>) -> vec2<f32> {\n  let directionLength = length(direction);\n  if (directionLength > 0.0) {\n    return direction / directionLength;\n  }\n\n  return vec2<f32>(1.0, 0.0);\n}\n\nfn water_getCoordinates(\n  position_worldspace: vec3<f32>,\n  position_objectspace: vec3<f32>,\n  uv: vec2<f32>\n) -> vec2<f32> {\n  var baseCoordinates = uv;\n  if (waterMaterial.mappingMode == 1) {\n    baseCoordinates = position_worldspace.xz;\n  } else if (waterMaterial.mappingMode == 2) {\n    let globeDirection = normalize(position_objectspace);\n    let longitude = atan2(globeDirection.x, globeDirection.z);\n    let latitude = asin(clamp(globeDirection.y, -1.0, 1.0));\n    baseCoordinates = vec2<f32>(longitude, latitude);\n  }\n\n  return baseCoordinates * waterMaterial.coordinateScale + waterMaterial.coordinateOffset;\n}\n\nfn water_getWaveGradient(\n  coordinates: vec2<f32>,\n  direction: vec2<f32>,\n  speed: f32,\n  frequency: f32,\n  amplitude: f32\n) -> vec2<f32> {\n  let normalizedDirection = water_getDirection(direction);\n  let phase = dot(coordinates * frequency, normalizedDirection) + waterMaterial.time * speed;\n  return cos(phase) * normalizedDirection * frequency * amplitude;\n}\n\nfn water_getTangent(normal_worldspace: vec3<f32>) -> vec3<f32> {\n  var referenceAxis = vec3<f32>(0.0, 0.0, 1.0);\n  if (abs(normal_worldspace.z) >= 0.999) {\n    referenceAxis = vec3<f32>(0.0, 1.0, 0.0);\n  }\n\n  return normalize(cross(referenceAxis, normal_worldspace));\n}\n\nfn water_getNormal(\n  position_worldspace: vec3<f32>,\n  position_objectspace: vec3<f32>,\n  normal_worldspace: vec3<f32>,\n  uv: vec2<f32>\n) -> vec3<f32> {\n  let coordinates = water_getCoordinates(position_worldspace, position_objectspace, uv);\n  let gradient =\n    water_getWaveGradient(\n      coordinates,\n      waterMaterial.waveADirection,\n      waterMaterial.waveASpeed,\n      waterMaterial.waveAFrequency,\n      waterMaterial.waveAAmplitude\n    ) +\n    water_getWaveGradient(\n      coordinates,\n      waterMaterial.waveBDirection,\n      waterMaterial.waveBSpeed,\n      waterMaterial.waveBFrequency,\n      waterMaterial.waveBAmplitude\n    );\n  let tangent = water_getTangent(normal_worldspace);\n  let bitangent = normalize(cross(normal_worldspace, tangent));\n  let perturbation =\n    waterMaterial.normalStrength * (gradient.x * tangent + gradient.y * bitangent);\n\n  return normalize(normal_worldspace + perturbation);\n}\n\nfn water_getSpecularContribution(\n  light_direction: vec3<f32>,\n  view_direction: vec3<f32>,\n  normal_worldspace: vec3<f32>,\n  light_color: vec3<f32>,\n  fresnel: f32\n) -> vec3<f32> {\n  let halfwayDirection = normalize(light_direction + view_direction);\n  let specular =\n    pow(max(dot(normal_worldspace, halfwayDirection), 0.0), 72.0) *\n    waterMaterial.specularIntensity *\n    (0.25 + 0.75 * fresnel);\n\n  return waterMaterial.fresnelColor * light_color * specular;\n}\n\nfn water_getColorMapped(\n  cameraPosition: vec3<f32>,\n  position_worldspace: vec3<f32>,\n  position_objectspace: vec3<f32>,\n  normal_worldspace: vec3<f32>,\n  uv: vec2<f32>\n) -> vec4<f32> {\n  let waterNormal = water_getNormal(\n    position_worldspace,\n    position_objectspace,\n    normalize(normal_worldspace),\n    uv\n  );\n  let viewDirection = normalize(cameraPosition - position_worldspace);\n  let fresnel =\n    pow(\n      1.0 - max(dot(viewDirection, waterNormal), 0.0),\n      max(waterMaterial.fresnelPower, 0.0001)\n    );\n  let surfaceColor = mix(\n    waterMaterial.baseColor,\n    waterMaterial.fresnelColor,\n    clamp(fresnel * 0.6, 0.0, 1.0)\n  );\n\n  if (lighting.enabled == 0) {\n    return vec4<f32>(surfaceColor, waterMaterial.opacity);\n  }\n\n  var lightColor = surfaceColor * (0.15 + 0.85 * lighting.ambientColor);\n\n  for (var i: i32 = 0; i < lighting.pointLightCount; i++) {\n    let pointLight = lighting_getPointLight(i);\n    let lightPosition = pointLight.position;\n    let lightDirection = normalize(lightPosition - position_worldspace);\n    let attenuation = getPointLightAttenuation(\n      pointLight,\n      distance(lightPosition, position_worldspace)\n    );\n    let incidentLight = pointLight.color / attenuation;\n    let diffuse = max(dot(waterNormal, lightDirection), 0.0);\n\n    lightColor += surfaceColor * incidentLight * diffuse;\n    lightColor += water_getSpecularContribution(\n      lightDirection,\n      viewDirection,\n      waterNormal,\n      incidentLight,\n      fresnel\n    );\n  }\n\n  for (var i: i32 = 0; i < lighting.spotLightCount; i++) {\n    let spotLight = lighting_getSpotLight(i);\n    let lightPosition = spotLight.position;\n    let lightDirection = normalize(lightPosition - position_worldspace);\n    let attenuation = getSpotLightAttenuation(spotLight, position_worldspace);\n    let incidentLight = spotLight.color / attenuation;\n    let diffuse = max(dot(waterNormal, lightDirection), 0.0);\n\n    lightColor += surfaceColor * incidentLight * diffuse;\n    lightColor += water_getSpecularContribution(\n      lightDirection,\n      viewDirection,\n      waterNormal,\n      incidentLight,\n      fresnel\n    );\n  }\n\n  for (var i: i32 = 0; i < lighting.directionalLightCount; i++) {\n    let directionalLight = lighting_getDirectionalLight(i);\n    let lightDirection = normalize(-directionalLight.direction);\n    let diffuse = max(dot(waterNormal, lightDirection), 0.0);\n\n    lightColor += surfaceColor * directionalLight.color * diffuse;\n    lightColor += water_getSpecularContribution(\n      lightDirection,\n      viewDirection,\n      waterNormal,\n      directionalLight.color,\n      fresnel\n    );\n  }\n\n  lightColor = mix(\n    lightColor,\n    waterMaterial.fresnelColor,\n    clamp(fresnel, 0.0, 1.0) * 0.35\n  );\n  return vec4<f32>(lightColor, waterMaterial.opacity);\n}\n\nfn water_getColor(\n  cameraPosition: vec3<f32>,\n  position_worldspace: vec3<f32>,\n  normal_worldspace: vec3<f32>,\n  uv: vec2<f32>\n) -> vec4<f32> {\n  return water_getColorMapped(\n    cameraPosition,\n    position_worldspace,\n    position_worldspace,\n    normal_worldspace,\n    uv\n  );\n}\n",vs:"layout(std140) uniform waterMaterialUniforms {\n  uniform float time;\n  uniform vec3 baseColor;\n  uniform float opacity;\n  uniform vec3 fresnelColor;\n  uniform float fresnelPower;\n  uniform float specularIntensity;\n  uniform float normalStrength;\n  uniform int mappingMode;\n  uniform vec2 coordinateScale;\n  uniform vec2 coordinateOffset;\n  uniform vec2 waveADirection;\n  uniform float waveASpeed;\n  uniform float waveAFrequency;\n  uniform float waveAAmplitude;\n  uniform vec2 waveBDirection;\n  uniform float waveBSpeed;\n  uniform float waveBFrequency;\n  uniform float waveBAmplitude;\n} waterMaterial;\n",fs:"layout(std140) uniform waterMaterialUniforms {\n  uniform float time;\n  uniform vec3 baseColor;\n  uniform float opacity;\n  uniform vec3 fresnelColor;\n  uniform float fresnelPower;\n  uniform float specularIntensity;\n  uniform float normalStrength;\n  uniform int mappingMode;\n  uniform vec2 coordinateScale;\n  uniform vec2 coordinateOffset;\n  uniform vec2 waveADirection;\n  uniform float waveASpeed;\n  uniform float waveAFrequency;\n  uniform float waveAAmplitude;\n  uniform vec2 waveBDirection;\n  uniform float waveBSpeed;\n  uniform float waveBFrequency;\n  uniform float waveBAmplitude;\n} waterMaterial;\n\nvec2 water_getDirection(vec2 direction) {\n  float directionLength = length(direction);\n  return directionLength > 0.0 ? direction / directionLength : vec2(1.0, 0.0);\n}\n\nvec2 water_getCoordinates(vec3 position_worldspace, vec3 position_objectspace, vec2 uv) {\n  vec2 baseCoordinates = uv;\n  if (waterMaterial.mappingMode == 1) {\n    baseCoordinates = position_worldspace.xz;\n  } else if (waterMaterial.mappingMode == 2) {\n    vec3 globeDirection = normalize(position_objectspace);\n    float longitude = atan(globeDirection.x, globeDirection.z);\n    float latitude = asin(clamp(globeDirection.y, -1.0, 1.0));\n    baseCoordinates = vec2(longitude, latitude);\n  }\n  return baseCoordinates * waterMaterial.coordinateScale + waterMaterial.coordinateOffset;\n}\n\nvec2 water_getWaveGradient(\n  vec2 coordinates,\n  vec2 direction,\n  float speed,\n  float frequency,\n  float amplitude\n) {\n  vec2 normalizedDirection = water_getDirection(direction);\n  float phase = dot(coordinates * frequency, normalizedDirection) + waterMaterial.time * speed;\n  return cos(phase) * normalizedDirection * frequency * amplitude;\n}\n\nvec3 water_getTangent(vec3 normal_worldspace) {\n  vec3 referenceAxis = abs(normal_worldspace.z) < 0.999 ? vec3(0.0, 0.0, 1.0) : vec3(0.0, 1.0, 0.0);\n  return normalize(cross(referenceAxis, normal_worldspace));\n}\n\nvec3 water_getNormal(\n  vec3 position_worldspace,\n  vec3 position_objectspace,\n  vec3 normal_worldspace,\n  vec2 uv\n) {\n  vec2 coordinates = water_getCoordinates(position_worldspace, position_objectspace, uv);\n  vec2 gradient =\n    water_getWaveGradient(\n      coordinates,\n      waterMaterial.waveADirection,\n      waterMaterial.waveASpeed,\n      waterMaterial.waveAFrequency,\n      waterMaterial.waveAAmplitude\n    ) +\n    water_getWaveGradient(\n      coordinates,\n      waterMaterial.waveBDirection,\n      waterMaterial.waveBSpeed,\n      waterMaterial.waveBFrequency,\n      waterMaterial.waveBAmplitude\n    );\n\n  vec3 tangent = water_getTangent(normal_worldspace);\n  vec3 bitangent = normalize(cross(normal_worldspace, tangent));\n  vec3 perturbation =\n    waterMaterial.normalStrength * (gradient.x * tangent + gradient.y * bitangent);\n\n  return normalize(normal_worldspace + perturbation);\n}\n\nvec3 water_getSpecularContribution(\n  vec3 light_direction,\n  vec3 view_direction,\n  vec3 normal_worldspace,\n  vec3 light_color,\n  float fresnel\n) {\n  vec3 halfway_direction = normalize(light_direction + view_direction);\n  float specular =\n    pow(max(dot(normal_worldspace, halfway_direction), 0.0), 72.0) *\n    waterMaterial.specularIntensity *\n    (0.25 + 0.75 * fresnel);\n\n  return waterMaterial.fresnelColor * light_color * specular;\n}\n\nvec4 water_getColorMapped(\n  vec3 cameraPosition,\n  vec3 position_worldspace,\n  vec3 position_objectspace,\n  vec3 normal_worldspace,\n  vec2 uv\n) {\n  vec3 waterNormal = water_getNormal(\n    position_worldspace,\n    position_objectspace,\n    normalize(normal_worldspace),\n    uv\n  );\n  vec3 viewDirection = normalize(cameraPosition - position_worldspace);\n  float fresnel =\n    pow(\n      1.0 - max(dot(viewDirection, waterNormal), 0.0),\n      max(waterMaterial.fresnelPower, 0.0001)\n    );\n  vec3 surfaceColor = mix(\n    waterMaterial.baseColor,\n    waterMaterial.fresnelColor,\n    clamp(fresnel * 0.6, 0.0, 1.0)\n  );\n\n  if (lighting.enabled == 0) {\n    return vec4(surfaceColor, waterMaterial.opacity);\n  }\n\n  vec3 lightColor = surfaceColor * (0.15 + 0.85 * lighting.ambientColor);\n\n  for (int i = 0; i < lighting.pointLightCount; i++) {\n    PointLight pointLight = lighting_getPointLight(i);\n    vec3 lightPosition = pointLight.position;\n    vec3 lightDirection = normalize(lightPosition - position_worldspace);\n    float attenuation =\n      getPointLightAttenuation(pointLight, distance(lightPosition, position_worldspace));\n    vec3 incidentLight = pointLight.color / attenuation;\n    float diffuse = max(dot(waterNormal, lightDirection), 0.0);\n\n    lightColor += surfaceColor * incidentLight * diffuse;\n    lightColor += water_getSpecularContribution(\n      lightDirection,\n      viewDirection,\n      waterNormal,\n      incidentLight,\n      fresnel\n    );\n  }\n\n  for (int i = 0; i < lighting.spotLightCount; i++) {\n    SpotLight spotLight = lighting_getSpotLight(i);\n    vec3 lightPosition = spotLight.position;\n    vec3 lightDirection = normalize(lightPosition - position_worldspace);\n    float attenuation = getSpotLightAttenuation(spotLight, position_worldspace);\n    vec3 incidentLight = spotLight.color / attenuation;\n    float diffuse = max(dot(waterNormal, lightDirection), 0.0);\n\n    lightColor += surfaceColor * incidentLight * diffuse;\n    lightColor += water_getSpecularContribution(\n      lightDirection,\n      viewDirection,\n      waterNormal,\n      incidentLight,\n      fresnel\n    );\n  }\n\n  for (int i = 0; i < lighting.directionalLightCount; i++) {\n    DirectionalLight directionalLight = lighting_getDirectionalLight(i);\n    vec3 lightDirection = normalize(-directionalLight.direction);\n    float diffuse = max(dot(waterNormal, lightDirection), 0.0);\n\n    lightColor += surfaceColor * directionalLight.color * diffuse;\n    lightColor += water_getSpecularContribution(\n      lightDirection,\n      viewDirection,\n      waterNormal,\n      directionalLight.color,\n      fresnel\n    );\n  }\n\n  lightColor = mix(lightColor, waterMaterial.fresnelColor, clamp(fresnel, 0.0, 1.0) * 0.35);\n  return vec4(lightColor, waterMaterial.opacity);\n}\n\nvec4 water_getColor(\n  vec3 cameraPosition,\n  vec3 position_worldspace,\n  vec3 normal_worldspace,\n  vec2 uv\n) {\n  return water_getColorMapped(\n    cameraPosition,\n    position_worldspace,\n    position_worldspace,\n    normal_worldspace,\n    uv\n  );\n}\n",defines:{LIGHTING_FRAGMENT:!0},uniformTypes:{time:"f32",baseColor:"vec3<f32>",opacity:"f32",fresnelColor:"vec3<f32>",fresnelPower:"f32",specularIntensity:"f32",normalStrength:"f32",mappingMode:"i32",coordinateScale:"vec2<f32>",coordinateOffset:"vec2<f32>",waveADirection:"vec2<f32>",waveASpeed:"f32",waveAFrequency:"f32",waveAAmplitude:"f32",waveBDirection:"vec2<f32>",waveBSpeed:"f32",waveBFrequency:"f32",waveBAmplitude:"f32"},defaultUniforms:g,getUniforms(e){var n,t,i,o,a,r,l,c,s,d,p,u,m;let f=arguments.length>1&&void 0!==arguments[1]?arguments[1]:g,w=e||{},{mapping:b}=w,x=function(e,n){if(null==e)return{};var t,i,o,a={};if("u">typeof Reflect&&Reflect.ownKeys){for(o=0,t=Reflect.ownKeys(Object(e));o<t.length;o++)i=t[o],!(n.indexOf(i)>=0)&&Object.prototype.propertyIsEnumerable.call(e,i)&&(a[i]=e[i]);return a}if(a=function(e,n){if(null==e)return{};var t,i,o={},a=Object.getOwnPropertyNames(e);for(i=0;i<a.length;i++)t=a[i],!(n.indexOf(t)>=0)&&Object.prototype.propertyIsEnumerable.call(e,t)&&(o[t]=e[t]);return o}(e,n),Object.getOwnPropertySymbols)for(o=0,t=Object.getOwnPropertySymbols(e);o<t.length;o++)i=t[o],!(n.indexOf(i)>=0)&&Object.prototype.propertyIsEnumerable.call(e,i)&&(a[i]=e[i]);return a}(w,["mapping"]),M={time:null!=(t=(n=f).time)?t:g.time,baseColor:n.baseColor?h(n.baseColor):g.baseColor,opacity:null!=(i=n.opacity)?i:g.opacity,fresnelColor:n.fresnelColor?h(n.fresnelColor):g.fresnelColor,fresnelPower:null!=(o=n.fresnelPower)?o:g.fresnelPower,specularIntensity:null!=(a=n.specularIntensity)?a:g.specularIntensity,normalStrength:null!=(r=n.normalStrength)?r:g.normalStrength,mappingMode:null!=(l=n.mappingMode)?l:g.mappingMode,coordinateScale:n.coordinateScale?[Number(n.coordinateScale[0]),Number(n.coordinateScale[1])]:g.coordinateScale,coordinateOffset:n.coordinateOffset?[Number(n.coordinateOffset[0]),Number(n.coordinateOffset[1])]:g.coordinateOffset,waveADirection:n.waveADirection?v(n.waveADirection):g.waveADirection,waveASpeed:null!=(c=n.waveASpeed)?c:g.waveASpeed,waveAFrequency:null!=(s=n.waveAFrequency)?s:g.waveAFrequency,waveAAmplitude:null!=(d=n.waveAAmplitude)?d:g.waveAAmplitude,waveBDirection:n.waveBDirection?v(n.waveBDirection):g.waveBDirection,waveBSpeed:null!=(p=n.waveBSpeed)?p:g.waveBSpeed,waveBFrequency:null!=(u=n.waveBFrequency)?u:g.waveBFrequency,waveBAmplitude:null!=(m=n.waveBAmplitude)?m:g.waveBAmplitude};return void 0!==x.time&&(M.time=x.time),void 0!==x.opacity&&(M.opacity=x.opacity),void 0!==x.fresnelPower&&(M.fresnelPower=x.fresnelPower),void 0!==x.specularIntensity&&(M.specularIntensity=x.specularIntensity),void 0!==x.normalStrength&&(M.normalStrength=x.normalStrength),void 0!==x.coordinateScale&&(M.coordinateScale=[Number(x.coordinateScale[0]),Number(x.coordinateScale[1])]),void 0!==x.coordinateOffset&&(M.coordinateOffset=[Number(x.coordinateOffset[0]),Number(x.coordinateOffset[1])]),void 0!==x.waveASpeed&&(M.waveASpeed=x.waveASpeed),void 0!==x.waveAFrequency&&(M.waveAFrequency=x.waveAFrequency),void 0!==x.waveAAmplitude&&(M.waveAAmplitude=x.waveAAmplitude),void 0!==x.waveBSpeed&&(M.waveBSpeed=x.waveBSpeed),void 0!==x.waveBFrequency&&(M.waveBFrequency=x.waveBFrequency),void 0!==x.waveBAmplitude&&(M.waveBAmplitude=x.waveBAmplitude),x.baseColor&&(M.baseColor=h(x.baseColor)),x.fresnelColor&&(M.fresnelColor=h(x.fresnelColor)),x.waveADirection&&(M.waveADirection=v(x.waveADirection)),x.waveBDirection&&(M.waveBDirection=v(x.waveBDirection)),void 0!==b&&(M.mappingMode="world"===b?1:2*("object"===b)),M}};function h(e){let n=[Number(e[0]),Number(e[1]),Number(e[2])];return Math.max(...n.map(e=>Math.abs(e)))>1&&(n[0]/=255,n[1]/=255,n[2]/=255),n}function v(e){let n=Number(e[0]),t=Number(e[1]),i=Math.hypot(n,t);return 0===i?[1,0]:[n/i,t/i]}var w=t(60434),b=t(54901),x=t(36375),M=t(79216),S=t(21803);let y=t.p+"assets/images/earth-81e0105e6588f39800ac80e38f75010b.jpg",C=t.p+"assets/images/earth-specular-b1d2b38996eff3d292257b040327d820.gif",L=t.p+"assets/images/tycho-negx-d8d212c530d4d50223699627a0326fab.jpg",D=t.p+"assets/images/tycho-negy-029f99f444ac0a48a899b9bd9ada62bb.jpg",P=t.p+"assets/images/tycho-negz-f1a637c0e22f5c5fe7054a02be2c2f79.jpg",_=t.p+"assets/images/tycho-posx-e504baff89acdb2986f4f3089ce6746e.jpg",k=t.p+"assets/images/tycho-posy-ee082ddd3cc5213f4e42ce6438bc30f1.jpg",z=t.p+"assets/images/tycho-posz-9c539d585e066a1e655c3173bfed5ede.jpg";function A(e,n,t){return n in e?Object.defineProperty(e,n,{value:t,enumerable:!0,configurable:!0,writable:!0}):e[n]=t,e}let N="#version 300 es\nprecision highp float;\n\nin vec3 positions;\nin vec3 normals;\nin vec2 texCoords;\n\nout vec2 vUV;\nout vec3 vPosition;\nout vec3 vNormal;\nout vec3 vLocalNormal;\nout vec3 vLocalPosition;\n\nuniform globeSceneUniforms {\n  mat4 viewProjectionMatrix;\n  mat4 modelMatrix;\n  mat4 normalMatrix;\n  vec3 cameraPosition;\n  int showLandTexture;\n  float oceanReflectionStrength;\n} globeScene;\n\nvoid main(void) {\n  vec4 worldPosition = globeScene.modelMatrix * vec4(positions, 1.0);\n  gl_Position = globeScene.viewProjectionMatrix * worldPosition;\n  vUV = texCoords;\n  vPosition = worldPosition.xyz;\n  vNormal = normalize((globeScene.normalMatrix * vec4(normals, 0.0)).xyz);\n  vLocalNormal = normalize(normals);\n  vLocalPosition = positions;\n}\n",I={name:"globeScene",uniformTypes:{viewProjectionMatrix:"mat4x4<f32>",modelMatrix:"mat4x4<f32>",normalMatrix:"mat4x4<f32>",cameraPosition:"vec3<f32>",showLandTexture:"i32",oceanReflectionStrength:"f32"}},T={name:"skyboxScene",uniformTypes:{modelMatrix:"mat4x4<f32>",viewMatrix:"mat4x4<f32>",projectionMatrix:"mat4x4<f32>",hdrStarsEnabled:"i32"}},B={starBackgroundEnabled:!0,hdrStarsEnabled:!0,waterEnabled:!0,landTextureEnabled:!0,waveSpeed:1.25,normalStrength:.52,fresnelPower:6.2,specularIntensity:1.8,oceanReflectionStrength:.2,lightAzimuth:-38,lightElevation:34},V=(0,b.F2)(23.44),F=(0,b.F2)(-90);class j extends i.J{async onInitialize(){this.panels.mount()}onFinalize(){this.settingsPanel.finalize(),this.panels.finalize(),this.backgroundModel.destroy(),this.landModel.destroy(),this.oceanModel.destroy(),this.tychoSkyTexture.destroy(),this.landMaterial.destroy(),this.oceanMaterial.destroy(),this.landTexture.destroy(),this.waterMaskTexture.destroy(),this.orbitControls.destroy()}onRender(e){let{aspect:n,device:t,time:i}=e;this.orbitControls.update(i);let o=new x.k().perspective({fovy:Math.PI/3.6,aspect:n,near:.1,far:100}),a=this.orbitControls.getEyePosition(),r=new x.k().lookAt({eye:a,center:[0,0,0]}),l=new x.k(r);l[12]=0,l[13]=0,l[14]=0;let c=t.beginRenderPass({clearColor:this.getClearColor(),clearDepth:1});this.controls.starBackgroundEnabled&&(this.backgroundShaderInputs.setProps({skyboxScene:{modelMatrix:new x.k().scale([40,40,40]),viewMatrix:l,projectionMatrix:o,hdrStarsEnabled:+!!this.controls.hdrStarsEnabled}}),this.backgroundModel.draw(c));let s=new x.k(o).multiplyRight(r),d=new x.k().rotateZ(V).rotateY(F+8e-5*i),p=new x.k(d).invert().transpose(),u=this.getLightingProps();this.landShaderInputs.setProps({globeScene:{viewProjectionMatrix:s,modelMatrix:d,normalMatrix:p,cameraPosition:a,showLandTexture:+!!this.controls.landTextureEnabled,oceanReflectionStrength:this.controls.oceanReflectionStrength},lighting:u}),this.oceanShaderInputs.setProps({globeScene:{viewProjectionMatrix:s,modelMatrix:d,normalMatrix:p,cameraPosition:a,showLandTexture:+!!this.controls.landTextureEnabled,oceanReflectionStrength:this.controls.oceanReflectionStrength},lighting:u}),this.oceanMaterial.setProps({waterMaterial:{time:i/1e3,normalStrength:this.controls.normalStrength,fresnelPower:this.controls.fresnelPower,specularIntensity:this.controls.specularIntensity,waveASpeed:.85*this.controls.waveSpeed,waveBSpeed:-1.4*this.controls.waveSpeed}}),this.landModel.draw(c),this.controls.waterEnabled&&this.oceanModel.draw(c),c.end()}getClearColor(){return[.01,.03,.06,1]}makePanel(){return new M.v({id:"globe-controls",title:"Controls",panels:[(0,S.TD)({id:"globe-description",title:"",html:'<p>Revives the classic Earth specular demo as a modern luma.gl v9 showcase with animated oceans and a starfield backdrop.</p>\n<p>Drag to orbit. Use the mouse wheel or trackpad to zoom.</p>\n<p style="font-size: 11px;">Sky: NASA <a href="https://science.nasa.gov/3d-resources/tycho-star-map/">Tycho Star Map</a>.</p>\n'}),this.settingsPanel.makePanel()]})}getLightingProps(){var e,n,t;let i,o,a,r=(e=this.controls.lightAzimuth,n=this.controls.lightElevation,i=(0,b.F2)(e),[-(Math.cos(o=(0,b.F2)(n))*Math.sin(i)),-Math.sin(o),-(Math.cos(o)*Math.cos(i))]);return{lights:[{type:"ambient",color:[255,255,255],intensity:.26},{type:"directional",color:[255,250,244],intensity:1.3,direction:r},{type:"directional",color:[152,198,255],intensity:.35,direction:0===(a=Math.hypot((t=[-(.6*r[0]),-.35,-(.6*r[2])])[0],t[1],t[2]))?[0,-1,0]:[t[0]/a,t[1]/a,t[2]/a]}]}}constructor({device:e}){super(),A(this,"backgroundModel",void 0),A(this,"landModel",void 0),A(this,"oceanModel",void 0),A(this,"landMaterial",void 0),A(this,"oceanMaterial",void 0),A(this,"backgroundShaderInputs",void 0),A(this,"landShaderInputs",void 0),A(this,"oceanShaderInputs",void 0),A(this,"tychoSkyTexture",void 0),A(this,"landTexture",void 0),A(this,"waterMaskTexture",void 0),A(this,"device",void 0),A(this,"controls",function(e){for(var n=1;n<arguments.length;n++){var t=null!=arguments[n]?arguments[n]:{},i=Object.keys(t);"function"==typeof Object.getOwnPropertySymbols&&(i=i.concat(Object.getOwnPropertySymbols(t).filter(function(e){return Object.getOwnPropertyDescriptor(t,e).enumerable}))),i.forEach(function(n){A(e,n,t[n])})}return e}({},B)),A(this,"orbitControls",void 0),A(this,"settingsPanel",void 0),A(this,"panels",void 0),A(this,"handleSettingsChange",(e,n)=>{for(let e of Object.keys(this.controls)){var t;let i=null==(t=(0,S.ZT)(n,e))?void 0:t.nextValue;("boolean"==typeof i||"number"==typeof i)&&(this.controls[e]=i)}}),this.device=e,this.settingsPanel=new S.$9({id:"globe-settings",schema:{title:"Settings",sections:[{id:"layers",name:"Layers",initiallyCollapsed:!1,settings:[{name:"starBackgroundEnabled",label:"Sky Background",type:"boolean",persist:"none"},{name:"hdrStarsEnabled",label:"HDR Stars",type:"boolean",persist:"none"},{name:"waterEnabled",label:"Water Overlay",type:"boolean",persist:"none"},{name:"landTextureEnabled",label:"Land Texture",type:"boolean",persist:"none"}]},{id:"water",name:"Water",initiallyCollapsed:!1,settings:[{name:"waveSpeed",label:"Wave Speed",type:"number",persist:"none",min:0,max:4,step:.01},{name:"normalStrength",label:"Normal Strength",type:"number",persist:"none",min:0,max:1.4,step:.01},{name:"fresnelPower",label:"Fresnel Power",type:"number",persist:"none",min:1,max:12,step:.1},{name:"specularIntensity",label:"Base Specular",type:"number",persist:"none",min:0,max:5,step:.05},{name:"oceanReflectionStrength",label:"HDR Reflection Boost",type:"number",persist:"none",min:0,max:2,step:.05}]},{id:"light",name:"Light",initiallyCollapsed:!1,settings:[{name:"lightAzimuth",label:"Light Azimuth",type:"number",persist:"none",min:-180,max:180,step:1},{name:"lightElevation",label:"Light Elevation",type:"number",persist:"none",min:5,max:85,step:1}]}]},settings:this.controls,onSettingsChange:this.handleSettingsChange}),this.panels=new S.c1({panel:this.makePanel()});let n=this.device.getDefaultCanvasContext().canvas;this.orbitControls=new o.N(n,{yaw:.6,pitch:.28,distance:2.9,minDistance:1.05,maxDistance:9,minPitch:-1.15,maxPitch:1.15,rotateSpeed:.01,zoomSpeed:.0015}),this.tychoSkyTexture=new a.R(e,{dimension:"cube",mipmaps:!0,mipLevels:"auto",data:(async()=>({"+X":await (0,r.Dt)(_),"-X":await (0,r.Dt)(L),"+Y":await (0,r.Dt)(k),"-Y":await (0,r.Dt)(D),"+Z":await (0,r.Dt)(z),"-Z":await (0,r.Dt)(P)}))(),sampler:{magFilter:"linear",minFilter:"linear",mipmapFilter:"nearest"}}),this.landTexture=new a.R(e,{data:(0,r.Dt)(y)}),this.waterMaskTexture=new a.R(e,{data:(0,r.Dt)(C)});let t=new l.G({radius:1,nlat:48,nlong:72}),i=new l.G({radius:1.002,nlat:48,nlong:72}),g=new c.YS(e,{modules:[u.w]}),h=new c.YS(e,{modules:[f]});this.landMaterial=g.createMaterial(),this.landMaterial.setProps({phongMaterial:{ambient:.2,diffuse:1,shininess:18,specularColor:[28,34,40]}}),this.oceanMaterial=h.createMaterial(),this.oceanMaterial.setProps({waterMaterial:{mapping:"uv",baseColor:[10,70,128],fresnelColor:[232,245,252],opacity:.84,coordinateScale:[1,6.8],coordinateOffset:[0,0],normalStrength:this.controls.normalStrength,fresnelPower:this.controls.fresnelPower,specularIntensity:this.controls.specularIntensity,waveADirection:[0,-1],waveAFrequency:3.8,waveAAmplitude:.024,waveBDirection:[0,-1],waveBFrequency:11.8,waveBAmplitude:.012}}),this.landShaderInputs=new s.l({globeScene:I,lighting:m.x,floatColors:w.$n}),this.landShaderInputs.setProps({floatColors:{useByteColors:!0}}),this.oceanShaderInputs=new s.l({globeScene:I,lighting:m.x}),this.backgroundShaderInputs=new s.l({skyboxScene:T}),this.backgroundModel=new d.K(e,{id:"globe-background",source:"struct SkyboxSceneUniforms {\n  modelMatrix: mat4x4<f32>,\n  viewMatrix: mat4x4<f32>,\n  projectionMatrix: mat4x4<f32>,\n  hdrStarsEnabled: i32,\n};\n\n@group(0) @binding(auto) var<uniform> skyboxScene : SkyboxSceneUniforms;\n@group(0) @binding(auto) var cubeTexture : texture_cube<f32>;\n@group(0) @binding(auto) var cubeTextureSampler : sampler;\n\nstruct VertexInputs {\n  @location(0) positions : vec3<f32>,\n};\n\nstruct FragmentInputs {\n  @builtin(position) position : vec4<f32>,\n  @location(0) direction : vec3<f32>,\n};\n\n@vertex\nfn vertexMain(inputs: VertexInputs) -> FragmentInputs {\n  var outputs : FragmentInputs;\n  outputs.position =\n    skyboxScene.projectionMatrix *\n    skyboxScene.viewMatrix *\n    skyboxScene.modelMatrix *\n    vec4<f32>(inputs.positions, 1.0);\n  outputs.direction = inputs.positions;\n  return outputs;\n}\n\n@fragment\nfn fragmentMain(inputs: FragmentInputs) -> @location(0) vec4<f32> {\n  let skyColor = textureSample(cubeTexture, cubeTextureSampler, normalize(inputs.direction)).rgb;\n  let starPeak = max(max(skyColor.r, skyColor.g), skyColor.b);\n  let hdrStarGain = 1.0 + smoothstep(0.1, 0.45, starPeak) * 5.0;\n  let starGain = select(1.0, hdrStarGain, skyboxScene.hdrStarsEnabled != 0);\n  return vec4<f32>(skyColor * starGain, 1.0);\n}\n",vs:"#version 300 es\nprecision highp float;\nprecision highp int;\n\nin vec3 positions;\n\nout vec3 vDirection;\n\nuniform skyboxSceneUniforms {\n  mat4 modelMatrix;\n  mat4 viewMatrix;\n  mat4 projectionMatrix;\n  int hdrStarsEnabled;\n} skyboxScene;\n\nvoid main(void) {\n  gl_Position =\n    skyboxScene.projectionMatrix *\n    skyboxScene.viewMatrix *\n    skyboxScene.modelMatrix *\n    vec4(positions, 1.0);\n  vDirection = positions;\n}\n",fs:"#version 300 es\nprecision highp float;\nprecision highp int;\n\nin vec3 vDirection;\n\nuniform samplerCube cubeTexture;\nuniform skyboxSceneUniforms {\n  mat4 modelMatrix;\n  mat4 viewMatrix;\n  mat4 projectionMatrix;\n  int hdrStarsEnabled;\n} skyboxScene;\n\nout vec4 fragColor;\n\nvoid main(void) {\n  vec3 skyColor = texture(cubeTexture, normalize(vDirection)).rgb;\n  float starPeak = max(max(skyColor.r, skyColor.g), skyColor.b);\n  float hdrStarGain = 1.0 + smoothstep(0.1, 0.45, starPeak) * 5.0;\n  float starGain = skyboxScene.hdrStarsEnabled != 0 ? hdrStarGain : 1.0;\n  fragColor = vec4(skyColor * starGain, 1.0);\n}\n",modules:[T],shaderInputs:this.backgroundShaderInputs,geometry:new p.c9({indices:!0}),bindings:{cubeTexture:this.tychoSkyTexture},parameters:{depthWriteEnabled:!1,depthCompare:"less-equal",cullMode:"front"}}),this.landModel=new d.K(e,{id:"globe-land",source:"struct GlobeSceneUniforms {\n  viewProjectionMatrix: mat4x4<f32>,\n  modelMatrix: mat4x4<f32>,\n  normalMatrix: mat4x4<f32>,\n  cameraPosition: vec3<f32>,\n  showLandTexture: i32,\n  oceanReflectionStrength: f32,\n};\n\n@group(0) @binding(auto) var<uniform> globeScene : GlobeSceneUniforms;\n@group(0) @binding(auto) var landTexture: texture_2d<f32>;\n@group(0) @binding(auto) var landTextureSampler: sampler;\n@group(0) @binding(auto) var landMaskTexture: texture_2d<f32>;\n@group(0) @binding(auto) var landMaskTextureSampler: sampler;\n\nstruct VertexInputs {\n  @location(0) positions : vec3<f32>,\n  @location(1) normals : vec3<f32>,\n  @location(2) texCoords : vec2<f32>,\n};\n\nstruct FragmentInputs {\n  @builtin(position) position : vec4<f32>,\n  @location(0) fragUV : vec2<f32>,\n  @location(1) fragPosition : vec3<f32>,\n  @location(2) fragNormal : vec3<f32>,\n  @location(3) fragLocalNormal : vec3<f32>,\n  @location(4) fragLocalPosition : vec3<f32>,\n};\n\n@vertex\nfn vertexMain(inputs: VertexInputs) -> FragmentInputs {\n  var outputs : FragmentInputs;\n  let worldPosition = globeScene.modelMatrix * vec4<f32>(inputs.positions, 1.0);\n\n  outputs.position = globeScene.viewProjectionMatrix * worldPosition;\n  outputs.fragUV = inputs.texCoords;\n  outputs.fragPosition = worldPosition.xyz;\n  outputs.fragNormal = normalize((globeScene.normalMatrix * vec4<f32>(inputs.normals, 0.0)).xyz);\n  outputs.fragLocalNormal = normalize(inputs.normals);\n  outputs.fragLocalPosition = inputs.positions;\n\n  return outputs;\n}\n\n@fragment\nfn fragmentMain(inputs: FragmentInputs) -> @location(0) vec4<f32> {\n  let surfaceUV = vec2<f32>(inputs.fragUV.x, 1.0 - inputs.fragUV.y);\n  let landSample = textureSample(landTexture, landTextureSampler, surfaceUV).rgb;\n  let landMaskSample = textureSample(landMaskTexture, landMaskTextureSampler, surfaceUV).r;\n  let landMask = smoothstep(0.4, 0.6, landMaskSample);\n  let textureMix = select(0.0, 1.0, globeScene.showLandTexture != 0);\n  var baseColor = mix(vec3<f32>(0.74, 0.72, 0.66), landSample, textureMix);\n  let normalizedNormal = normalize(inputs.fragNormal);\n  let polarNormal = normalize(inputs.fragLocalNormal);\n  let latitudeMask = smoothstep(0.45, 0.72, abs(polarNormal.y));\n  let southPolarMask = smoothstep(0.58, 0.82, -polarNormal.y);\n  let landLuminance = dot(landSample, vec3<f32>(0.2126, 0.7152, 0.0722));\n  let brightIceMask = latitudeMask * smoothstep(0.68, 0.9, landLuminance);\n  let iceMask = textureMix * landMask * max(brightIceMask, southPolarMask);\n  baseColor = mix(baseColor, vec3<f32>(0.95, 0.97, 1.0), iceMask);\n  var litColor = lighting_getLightColor2(\n    baseColor,\n    globeScene.cameraPosition,\n    inputs.fragPosition,\n    normalizedNormal\n  );\n  var sunVisibility = 1.0;\n  let viewDirection = normalize(globeScene.cameraPosition - inputs.fragPosition);\n  let iceRim = pow(1.0 - max(dot(viewDirection, normalizedNormal), 0.0), 6.0);\n  var iceHighlight = vec3<f32>(0.0, 0.0, 0.0);\n\n  if (lighting.directionalLightCount > 0) {\n    let directionalLight = lighting_getDirectionalLight(0);\n    let lightDirection = normalize(-directionalLight.direction);\n    sunVisibility = smoothstep(-0.22, 0.28, dot(normalizedNormal, lightDirection));\n    let halfVector = normalize(lightDirection + viewDirection);\n    let directionalSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 96.0);\n    iceHighlight += directionalLight.color * directionalSpecular * 1.6;\n  }\n\n  if (lighting.pointLightCount > 0) {\n    let pointLight = lighting_getPointLight(0);\n    let lightDirection = normalize(pointLight.position - inputs.fragPosition);\n    let halfVector = normalize(lightDirection + viewDirection);\n    let directionalSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 96.0);\n    iceHighlight += pointLight.color * directionalSpecular * 0.8;\n  }\n\n  litColor += vec3<f32>(0.9, 0.96, 1.0) * iceMask * (iceRim * 0.55) + iceHighlight * iceMask;\n  litColor *= mix(0.22, 1.0, sunVisibility);\n  return vec4<f32>(litColor, 1.0);\n}\n",vs:N,fs:"#version 300 es\nprecision highp float;\n\nin vec2 vUV;\nin vec3 vPosition;\nin vec3 vNormal;\nin vec3 vLocalNormal;\n\nuniform sampler2D landTexture;\nuniform sampler2D landMaskTexture;\n\nuniform globeSceneUniforms {\n  mat4 viewProjectionMatrix;\n  mat4 modelMatrix;\n  mat4 normalMatrix;\n  vec3 cameraPosition;\n  int showLandTexture;\n  float oceanReflectionStrength;\n} globeScene;\n\nout vec4 fragColor;\n\nvoid main(void) {\n  vec2 surfaceUV = vec2(vUV.x, 1.0 - vUV.y);\n  vec3 landSample = texture(landTexture, surfaceUV).rgb;\n  float landMaskSample = texture(landMaskTexture, surfaceUV).r;\n  float landMask = smoothstep(0.4, 0.6, landMaskSample);\n  float textureMix = globeScene.showLandTexture != 0 ? 1.0 : 0.0;\n  vec3 baseColor = mix(vec3(0.74, 0.72, 0.66), landSample, textureMix);\n  vec3 normalizedNormal = normalize(vNormal);\n  vec3 polarNormal = normalize(vLocalNormal);\n  float latitudeMask = smoothstep(0.45, 0.72, abs(polarNormal.y));\n  float southPolarMask = smoothstep(0.58, 0.82, -polarNormal.y);\n  float landLuminance = dot(landSample, vec3(0.2126, 0.7152, 0.0722));\n  float brightIceMask = latitudeMask * smoothstep(0.68, 0.9, landLuminance);\n  float iceMask = textureMix * landMask * max(brightIceMask, southPolarMask);\n  baseColor = mix(baseColor, vec3(0.95, 0.97, 1.0), iceMask);\n  vec3 litColor = lighting_getLightColor(baseColor, globeScene.cameraPosition, vPosition, normalizedNormal);\n  float sunVisibility = 1.0;\n  vec3 viewDirection = normalize(globeScene.cameraPosition - vPosition);\n  float iceRim = pow(1.0 - max(dot(viewDirection, normalizedNormal), 0.0), 6.0);\n  vec3 iceHighlight = vec3(0.0);\n\n  if (lighting.directionalLightCount > 0) {\n    DirectionalLight directionalLight = lighting_getDirectionalLight(0);\n    vec3 lightDirection = normalize(-directionalLight.direction);\n    sunVisibility = smoothstep(-0.22, 0.28, dot(normalizedNormal, lightDirection));\n    vec3 halfVector = normalize(lightDirection + viewDirection);\n    float directionalSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 96.0);\n    iceHighlight += directionalLight.color * directionalSpecular * 1.6;\n  }\n\n  if (lighting.pointLightCount > 0) {\n    PointLight pointLight = lighting_getPointLight(0);\n    vec3 lightDirection = normalize(pointLight.position - vPosition);\n    vec3 halfVector = normalize(lightDirection + viewDirection);\n    float directionalSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 96.0);\n    iceHighlight += pointLight.color * directionalSpecular * 0.8;\n  }\n\n  litColor += vec3(0.9, 0.96, 1.0) * iceMask * (iceRim * 0.55) + iceHighlight * iceMask;\n  litColor *= mix(0.22, 1.0, sunVisibility);\n  fragColor = vec4(litColor, 1.0);\n}\n",modules:[I,m.x,u.w],shaderInputs:this.landShaderInputs,material:this.landMaterial,geometry:t,bindings:{landTexture:this.landTexture,landMaskTexture:this.waterMaskTexture},parameters:{depthWriteEnabled:!0,depthCompare:"less-equal",cullMode:"back"}}),this.oceanModel=new d.K(e,{id:"globe-ocean",source:"struct GlobeSceneUniforms {\n  viewProjectionMatrix: mat4x4<f32>,\n  modelMatrix: mat4x4<f32>,\n  normalMatrix: mat4x4<f32>,\n  cameraPosition: vec3<f32>,\n  showLandTexture: i32,\n  oceanReflectionStrength: f32,\n};\n\n@group(0) @binding(auto) var<uniform> globeScene : GlobeSceneUniforms;\n@group(0) @binding(auto) var waterMaskTexture: texture_2d<f32>;\n@group(0) @binding(auto) var waterMaskTextureSampler: sampler;\n\nstruct VertexInputs {\n  @location(0) positions : vec3<f32>,\n  @location(1) normals : vec3<f32>,\n  @location(2) texCoords : vec2<f32>,\n};\n\nstruct FragmentInputs {\n  @builtin(position) position : vec4<f32>,\n  @location(0) fragUV : vec2<f32>,\n  @location(1) fragPosition : vec3<f32>,\n  @location(2) fragNormal : vec3<f32>,\n  @location(3) fragLocalNormal : vec3<f32>,\n  @location(4) fragLocalPosition : vec3<f32>,\n};\n\n@vertex\nfn vertexMain(inputs: VertexInputs) -> FragmentInputs {\n  var outputs : FragmentInputs;\n  let worldPosition = globeScene.modelMatrix * vec4<f32>(inputs.positions, 1.0);\n\n  outputs.position = globeScene.viewProjectionMatrix * worldPosition;\n  outputs.fragUV = inputs.texCoords;\n  outputs.fragPosition = worldPosition.xyz;\n  outputs.fragNormal = normalize((globeScene.normalMatrix * vec4<f32>(inputs.normals, 0.0)).xyz);\n  outputs.fragLocalNormal = normalize(inputs.normals);\n  outputs.fragLocalPosition = inputs.positions;\n\n  return outputs;\n}\n\n@fragment\nfn fragmentMain(inputs: FragmentInputs) -> @location(0) vec4<f32> {\n  let surfaceUV = vec2<f32>(inputs.fragUV.x, 1.0 - inputs.fragUV.y);\n  let landMask = textureSample(waterMaskTexture, waterMaskTextureSampler, surfaceUV).r;\n  let oceanMask = smoothstep(0.4, 0.6, 1.0 - landMask);\n  let normalizedNormal = normalize(inputs.fragNormal);\n  let polarNormal = normalize(inputs.fragLocalNormal);\n  let polarIceMask = oceanMask * smoothstep(0.8, 0.92, abs(polarNormal.y));\n  let openWaterMask = max(oceanMask - polarIceMask, 0.0);\n  let waterColor = water_getColorMapped(\n    globeScene.cameraPosition,\n    inputs.fragPosition,\n    inputs.fragLocalPosition,\n    normalizedNormal,\n    surfaceUV\n  );\n  let waterLuminance = dot(waterColor.rgb, vec3<f32>(0.2126, 0.7152, 0.0722));\n  let waterReflectionGain =\n    1.0 + smoothstep(0.6, 0.95, waterLuminance) * globeScene.oceanReflectionStrength;\n  let oceanColor = waterColor.rgb * waterReflectionGain;\n  var sunVisibility = 1.0;\n  let viewDirection = normalize(globeScene.cameraPosition - inputs.fragPosition);\n  let iceRim = pow(1.0 - max(dot(viewDirection, normalizedNormal), 0.0), 5.0);\n  var iceHighlight = vec3<f32>(0.0, 0.0, 0.0);\n\n  if (lighting.directionalLightCount > 0) {\n    let directionalLight = lighting_getDirectionalLight(0);\n    let lightDirection = normalize(-directionalLight.direction);\n    sunVisibility = smoothstep(-0.22, 0.28, dot(normalizedNormal, lightDirection));\n    let halfVector = normalize(lightDirection + viewDirection);\n    let iceSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 88.0);\n    iceHighlight += directionalLight.color * iceSpecular * 1.55;\n  }\n\n  if (lighting.pointLightCount > 0) {\n    let pointLight = lighting_getPointLight(0);\n    let lightDirection = normalize(pointLight.position - inputs.fragPosition);\n    let halfVector = normalize(lightDirection + viewDirection);\n    let iceSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 88.0);\n    iceHighlight += pointLight.color * iceSpecular * 0.8;\n  }\n\n  let iceColor = vec3<f32>(0.95, 0.98, 1.0) + vec3<f32>(0.85, 0.92, 1.0) * iceRim * 0.28 + iceHighlight;\n  let finalColor =\n    (oceanColor * openWaterMask + iceColor * polarIceMask) *\n    mix(0.18, 1.0, sunVisibility);\n  let finalAlpha = waterColor.a * openWaterMask + 0.96 * polarIceMask;\n  return vec4<f32>(finalColor, finalAlpha);\n}\n",vs:N,fs:"#version 300 es\nprecision highp float;\n\nin vec2 vUV;\nin vec3 vPosition;\nin vec3 vNormal;\nin vec3 vLocalNormal;\nin vec3 vLocalPosition;\n\nuniform sampler2D waterMaskTexture;\n\nuniform globeSceneUniforms {\n  mat4 viewProjectionMatrix;\n  mat4 modelMatrix;\n  mat4 normalMatrix;\n  vec3 cameraPosition;\n  int showLandTexture;\n  float oceanReflectionStrength;\n} globeScene;\n\nout vec4 fragColor;\n\nvoid main(void) {\n  vec2 surfaceUV = vec2(vUV.x, 1.0 - vUV.y);\n  float landMask = texture(waterMaskTexture, surfaceUV).r;\n  float oceanMask = smoothstep(0.4, 0.6, 1.0 - landMask);\n  vec3 normalizedNormal = normalize(vNormal);\n  vec3 polarNormal = normalize(vLocalNormal);\n  float polarIceMask = oceanMask * smoothstep(0.8, 0.92, abs(polarNormal.y));\n  float openWaterMask = max(oceanMask - polarIceMask, 0.0);\n  vec4 waterColor = water_getColorMapped(\n    globeScene.cameraPosition,\n    vPosition,\n    vLocalPosition,\n    normalizedNormal,\n    surfaceUV\n  );\n  float waterLuminance = dot(waterColor.rgb, vec3(0.2126, 0.7152, 0.0722));\n  float waterReflectionGain =\n    1.0 + smoothstep(0.6, 0.95, waterLuminance) * globeScene.oceanReflectionStrength;\n  vec3 oceanColor = waterColor.rgb * waterReflectionGain;\n  float sunVisibility = 1.0;\n  vec3 viewDirection = normalize(globeScene.cameraPosition - vPosition);\n  float iceRim = pow(1.0 - max(dot(viewDirection, normalizedNormal), 0.0), 5.0);\n  vec3 iceHighlight = vec3(0.0);\n\n  if (lighting.directionalLightCount > 0) {\n    DirectionalLight directionalLight = lighting_getDirectionalLight(0);\n    vec3 lightDirection = normalize(-directionalLight.direction);\n    sunVisibility = smoothstep(-0.22, 0.28, dot(normalizedNormal, lightDirection));\n    vec3 halfVector = normalize(lightDirection + viewDirection);\n    float iceSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 88.0);\n    iceHighlight += directionalLight.color * iceSpecular * 1.55;\n  }\n\n  if (lighting.pointLightCount > 0) {\n    PointLight pointLight = lighting_getPointLight(0);\n    vec3 lightDirection = normalize(pointLight.position - vPosition);\n    vec3 halfVector = normalize(lightDirection + viewDirection);\n    float iceSpecular = pow(max(dot(normalizedNormal, halfVector), 0.0), 88.0);\n    iceHighlight += pointLight.color * iceSpecular * 0.8;\n  }\n\n  vec3 iceColor =\n    vec3(0.95, 0.98, 1.0) +\n    vec3(0.85, 0.92, 1.0) * iceRim * 0.28 +\n    iceHighlight;\n  vec3 finalColor =\n    oceanColor * openWaterMask +\n    iceColor * polarIceMask;\n  finalColor *= mix(0.18, 1.0, sunVisibility);\n  float finalAlpha = waterColor.a * openWaterMask + 0.96 * polarIceMask;\n  fragColor = vec4(finalColor, finalAlpha);\n}\n",modules:[I,m.x,f],shaderInputs:this.oceanShaderInputs,material:this.oceanMaterial,geometry:i,bindings:{waterMaskTexture:this.waterMaskTexture},parameters:{blend:!0,blendColorOperation:"add",blendColorSrcFactor:"src-alpha",blendColorDstFactor:"one-minus-src-alpha",blendAlphaOperation:"add",blendAlphaSrcFactor:"one",blendAlphaDstFactor:"one-minus-src-alpha",depthWriteEnabled:!1,depthCompare:"less-equal",cullMode:"back"}})}}A(j,"info",(0,S.S8)())},79216(e,n,t){t.d(n,{v:()=>r});var i=t(81413),o=t(6315),a=t(82717);class r extends a.Z{constructor({panels:e,id:n="column-panels",title:t="Panels",theme:o="inherit"}){super({id:n,title:t,theme:o,content:(0,i.Y)(l,{panels:e})})}}function l({panels:e,className:n}){return(0,i.Y)("div",{className:n,style:c,children:e.map((e,n)=>(0,i.FD)("section",{style:{...s,borderTop:n>0?"1px solid var(--menu-border, rgba(148, 163, 184, 0.25))":"none",opacity:e.disabled?.55:1},children:[e.title?(0,i.Y)("header",{style:d,children:e.title}):null,(0,i.Y)("div",{style:p,children:(0,i.Y)(o.U,{panel:e,children:e.content})})]},e.id))})}let c={display:"grid",gap:"0"},s={display:"grid",gap:"10px",padding:"12px 0"},d={fontSize:"12px",fontWeight:700,color:"var(--button-text, currentColor)",padding:"0 2px"},p={minWidth:0}}}]);