// luma.gl
// SPDX-License-Identifier: MIT
// SPDX-FileCopyrightText: Copyright (c) vis.gl contributors

import {AnimationLoopTemplate,AnimationLoop}from'@luma.gl/engine';import{WebGPUDevice}from'@luma.gl/webgpu';import{SpectralWaveEngine}from'./spectral-wave-engine';import{SpectralWaveRenderer}from'./spectral-wave-renderer';import{SpectralViewRenderer}from'./spectral-view-renderer';
class SpectralWaveLab extends AnimationLoopTemplate{engine!:SpectralWaveEngine;physical!:SpectralWaveRenderer;fourier!:SpectralViewRenderer;async onInitialize({device}:{device:WebGPUDevice}){this.engine=new SpectralWaveEngine(device,{resolution:256});this.physical=new SpectralWaveRenderer(device,this.engine.field,this.engine.resolution);this.fourier=new SpectralViewRenderer(device,this.engine.evolvedSpectrum,this.engine.resolution);writeInfo(this.engine);return{};}onRender({device,tick}:any){const time=tick/60;this.engine.encode(time);const canvas=device.getDefaultCanvasContext();const[w,h]=canvas.getDrawingBufferSize();const pass=device.beginRenderPass({id:'spectral-dual-view',framebuffer:canvas.getCurrentFramebuffer(),clearColor:[.003,.006,.018,1],clearDepth:1});pass.end();this.physical.render(time,[0,0,Math.ceil(w*.58),h]);const pass2=device.beginRenderPass({id:'spectral-fourier-view',framebuffer:canvas.getCurrentFramebuffer(),clearColor:false});this.fourier.draw(pass2,[Math.ceil(w*.58),0,w-Math.ceil(w*.58),h],time);pass2.end();}onFinalize(){this.fourier?.destroy();this.physical?.destroy();this.engine?.destroy();}}
function writeInfo(engine:SpectralWaveEngine):void{const e=document.getElementById('info');if(!e)return;const s=engine.stats;e.innerHTML=`<h1>Spectral Lab — Two Views of One Wave</h1><p><strong>Left: physical space.</strong> The height field is the wave as you would observe it: peaks, troughs and interference spreading through the domain.</p><p><strong>Right: Fourier space.</strong> The glowing map is the same state expressed as spatial frequencies. The center represents long wavelengths; farther from the center are progressively finer structures. Brightness shows spectral energy.</p><p>The important idea is that these are <em>not two simulations</em>. They are two representations of the same GPU state, connected every frame by a 2D inverse FFT.</p><div class="equation">∂²u/∂t² = c²Δu<br>û(k,t) = û₀(k) cos(c|k|t)</div><p>In physical space the Laplacian couples neighboring points. In Fourier space it becomes multiplication by −|k|², so every Fourier mode evolves independently. The right-hand view lets you literally watch that simplification.</p><h2>Follow the dynamics</h2><p>As rings collide on the left, look for where their energy lives on the right. Broad structures concentrate near the spectral center; sharp features populate higher spatial frequencies. Interference that looks complicated in physical space is simply the superposition of independent modes.</p><h2>Engineering</h2><dl><dt>Grid</dt><dd>${s.resolution} × ${s.resolution}</dd><dt>Fourier modes</dt><dd>${s.elementCount.toLocaleString()}</dd><dt>FFT passes</dt><dd>${s.fftPasses}</dd><dt>FFT dispatches/frame</dt><dd>${s.fftDispatchesPerFrame}</dd><dt>Wave speed</dt><dd>${s.waveSpeed}</dd></dl><p class="foot">One forward FFT captures the initial disturbance. Each frame evolves the spectrum analytically, renders that spectrum directly, inverse-transforms it, and renders the resulting physical field. Compute and visualization share the same GPU buffers.</p>`;}
AnimationLoop.start(new SpectralWaveLab(),{device:WebGPUDevice});
