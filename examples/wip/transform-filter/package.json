{
  "name": "luma.gl-examples-filter",
  "description": "Texture filtering using Transform",
  "scripts": {
    "start": "webpack-dev-server --progress --hot --open -d",
    "start-local": "webpack-dev-server --env.local --progress --hot --open -d"
  },
  "dependencies": {
    "luma.gl": "^6.1.0"
  },
  "devDependencies": {
    "webpack": "^4.3.0",
    "webpack-dev-server": "^3.1.1"
  }
}
