---
title: Experimental GPU Tables
description: Model batch-preserving GPU tables, schemas, bindings, ownership, and lifecycle through the private experimental subpath.
---

import {DocumentationBadge, DocumentationBadges} from '@site/src/components/docs/documentation-badges';
import {GPUTablesDocsTabs} from '@site/src/components/docs/gpu-tables-docs-tabs';

# GPU Tables

<GPUTablesDocsTabs active="overview" />

<DocumentationBadges>
  <DocumentationBadge tone="version">From v9.4</DocumentationBadge>
  <DocumentationBadge tone="experimental">Experimental API</DocumentationBadge>
</DocumentationBadges>

`@luma.gl/experimental/gpu-tables` provides experimental batch-preserving table helpers for rendering,
transforms, and compute. It builds on the primitive objects in
[`@luma.gl/gpgpu/gpu-data`](/docs/api-reference/gpgpu/gpu-data).

## Start Here

- Read the [GPU Tables guide](/docs/api-guide/gpu/gpu-tables) for end-to-end
  attribute and storage workflows.
- Read [GPU Table Structure](/docs/api-reference/experimental/gpu-tables/gpu-table-structure) for
  the logical-column and physical-batch object model.
- Use [`GPUTable`](/docs/api-reference/experimental/gpu-tables/gpu-table) for data ownership and
  [`GPUTableShaderBindings`](/docs/api-reference/experimental/gpu-tables/gpu-table-shader-bindings)
  for backend-specific shader resources.
- Use `@luma.gl/arrow` adapters when source data is Apache Arrow.

## API Reference

- [GPU Table Structure](/docs/api-reference/experimental/gpu-tables/gpu-table-structure)
- [GPU Table Lifecycle](/docs/api-reference/experimental/gpu-tables/gpu-table-lifecycle)
- [GPUTable](/docs/api-reference/experimental/gpu-tables/gpu-table)
- [GPURecordBatch](/docs/api-reference/experimental/gpu-tables/gpu-record-batch)
- [GPUSchema](/docs/api-reference/experimental/gpu-tables/gpu-schema)
- [GPUInputSchema](/docs/api-reference/experimental/gpu-tables/gpu-input-schema)
- [GPUTable Shader Bindings](/docs/api-reference/experimental/gpu-tables/gpu-table-shader-bindings)
- [GPUTableBufferPlanner](/docs/api-reference/experimental/gpu-tables/gpu-table-buffer-planner)
- [GPGPU Data Primitives](/docs/api-reference/gpgpu/gpu-data)
- [Supported Arrow Types](/docs/api-reference/arrow/supported-arrow-types)

The subpath owns `GPURecordBatch`, `GPUTable`, `GPUSchema`, `GPUField`, `GPUTypeMap`, table
bindings, table computations, and generic table planners. Models can publish `GPUInputSchema`
declarations for the prepared table inputs they accept. `GPUTableShaderBindings`
resolves those declarations into owned, batch-preserving attribute and storage
resources for a shader layout. Table-oriented execution helpers include
`TableTransform`,
`GPUTableComputation`, `GPUTableBufferPlanner`, and generated-buffer batching
utilities. `GPUTableModel` renders preserved table batches through one model pipeline, while
`GPUTableGeometry` exposes a packed static table as ordinary GPU geometry. Specialized path and
polygon models live separately in `@luma.gl/experimental/models`.

Arrow-specific construction and analysis helpers live in `@luma.gl/arrow`.
Applications that ingest Apache Arrow data should use those adapters to build
the generic GPU table objects exposed here.

## Installing

This subpath is experimental in 9.4 and is installed with its containing package. Install GPGPU as
well when constructing primitive data objects directly:

```bash
npm install @luma.gl/gpgpu @luma.gl/experimental
```
