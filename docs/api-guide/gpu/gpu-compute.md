# Using GPU Compute

This page is a placeholder for a fuller compute guide.

For now, the best related references are:

- [GPU Commands](/docs/api-guide/gpu/gpu-commands) for command encoding and submission flow.
- [GPU Rendering](/docs/api-guide/gpu/gpu-rendering) for how compute work fits alongside render passes.
- [Storage Buffers](/docs/api-guide/gpu/gpu-storage-buffers) for compute-oriented data access patterns.
- [ComputePipeline](/docs/api-reference/core/resources/compute-pipeline) for the core resource API.

## Scope

The eventual guide should cover:

- When to use compute instead of vertex or fragment shader work.
- How to structure compute inputs and outputs with bindings and storage buffers.
- How compute pipelines, passes, command encoders, and submission fit together.
- Portability considerations between WebGPU and WebGL fallbacks.

## Status

Content to be expanded.
